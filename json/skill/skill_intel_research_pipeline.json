{
  "name": "intel_research_pipeline",
  "description": "全网情报检索与汇总技能。针对一个主题并行执行网页与新闻检索，必要时抓取正文并产出结构化情报摘要。",
  "version": "1.0.0",
  "owner": "System",
  "beanName": "orchestrationSkills",
  "methodName": "intelResearchPipeline",
  "runMode": "ASYNC",
  "requiredCapabilities": [
    "WEB_SEARCH",
    "NEWS_SEARCH",
    "WEB_FETCH",
    "INTEL_SUMMARIZE"
  ],
  "toolSlots": [
    {
      "slot": "web",
      "capability": "WEB_SEARCH",
      "required": true
    },
    {
      "slot": "news",
      "capability": "NEWS_SEARCH",
      "required": true
    },
    {
      "slot": "fetch",
      "capability": "WEB_FETCH",
      "required": false
    },
    {
      "slot": "summarize",
      "capability": "INTEL_SUMMARIZE",
      "required": true
    }
  ],
  "thought_chain": [
    "解析 topic、timeRange、maxSources、needDeepScrape、outputStyle，并填充默认值（timeRange=7D，maxSources=12，needDeepScrape=true，outputStyle=BULLET）。",
    "并行调用【web】与【news】槽位构建候选情报源池。",
    "按主题相关性、来源可信度、发布时间和去重规则进行打分排序，保留前 maxSources 条。",
    "当 needDeepScrape=true 时，调用【fetch】槽位抓取高分来源正文，并记录抓取成功/失败状态。",
    "对摘要与正文做事实归并：抽取关键事件、主体、时间、数字与观点，标记冲突信息与不确定项。",
    "调用【summarize】槽位生成情报产出：summary 主文、highlights 要点、sources 来源清单；输出风格按 BULLET/REPORT/TIMELINE 组织。",
    "若来源不足或抓取大面积失败，返回部分成功结果并明确缺口，不伪造事实。",
    "返回结构化结果（status、summary、sources、highlights），确保可直接用于前端展示或后续分析。"
  ],
  "inputSchema": {
    "type": "object",
    "properties": {
      "topic": {
        "type": "string",
        "description": "情报主题（必填），例如：'广州低空经济政策进展'。"
      },
      "timeRange": {
        "type": "string",
        "enum": ["24H", "7D", "30D", "ALL"],
        "description": "时间范围（可选）。可选值：24H=近24小时；7D=近7天；30D=近30天；ALL=不限。默认 7D。"
      },
      "maxSources": {
        "type": "integer",
        "description": "最大来源数量（可选），建议 5-30，默认 12。"
      },
      "needDeepScrape": {
        "type": "boolean",
        "description": "是否抓取正文（可选）。true=对候选来源进行网页抓取；false=仅基于搜索摘要。默认 true。"
      },
      "outputStyle": {
        "type": "string",
        "enum": ["BULLET", "REPORT", "TIMELINE"],
        "description": "输出风格（可选）。BULLET=要点列表；REPORT=报告体；TIMELINE=时间线。默认 BULLET。"
      }
    },
    "required": ["topic"]
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "status": {
        "type": "string",
        "description": "执行状态，常见值：success 或 error。"
      },
      "summary": {
        "type": "string",
        "description": "情报汇总正文。"
      },
      "sources": {
        "type": "array",
        "description": "来源列表，元素通常包含 title/link/snippet/publishedAt 等。"
      },
      "highlights": {
        "type": "array",
        "description": "关键信息点列表。"
      }
    }
  }
}

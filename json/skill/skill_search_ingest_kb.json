{
  "name": "search_ingest_kb",
  "description": "搜索即入库技能。先搜索并抓取目标内容，再按规则清洗并写入知识库，形成可持续复用的 RAG 资产。",
  "version": "1.0.0",
  "owner": "System",
  "beanName": "orchestrationSkills",
  "methodName": "searchIngestKnowledgeBase",
  "runMode": "ASYNC",
  "inputSchema": {
    "type": "object",
    "properties": {
      "query": {
        "type": "string",
        "description": "搜索关键词（必填），例如：'Luna 桌面智能体架构设计'。"
      },
      "kbTitle": {
        "type": "string",
        "description": "入库标题（可选）。未传时可由系统自动生成。"
      },
      "sourceType": {
        "type": "string",
        "enum": ["WEB_SEARCH", "MANUAL_INPUT", "FILE"],
        "description": "入库来源类型（可选）。可选值：WEB_SEARCH=网络检索；MANUAL_INPUT=人工输入；FILE=文件导入。默认 WEB_SEARCH。"
      },
      "maxFetch": {
        "type": "integer",
        "description": "搜索抓取数量上限（可选），建议 1-20，默认 8。"
      },
      "deduplicate": {
        "type": "boolean",
        "description": "是否去重（可选）。true=按URL/标题哈希去重；false=不去重。默认 true。"
      }
    },
    "required": ["query"]
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "status": {
        "type": "string",
        "description": "执行状态，常见值：success 或 error。"
      },
      "insertedCount": {
        "type": "integer",
        "description": "成功写入知识库的分片/条目数量。"
      },
      "skippedCount": {
        "type": "integer",
        "description": "被跳过的数量（去重、空内容、抓取失败等）。"
      },
      "sourceList": {
        "type": "array",
        "description": "处理过的来源列表。"
      }
    }
  }
}

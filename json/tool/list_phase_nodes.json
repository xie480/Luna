{
  "name": "list_phase_nodes",
  "description": "按计划与阶段查询节点明细列表。用于阶段执行前拉取待执行节点、前端渲染流程图、故障排查定位具体节点状态。返回结果通常包含节点ID、类型、状态、依赖关系、重试信息等字段，是阶段调度与可观测面板的基础数据来源。建议与 query_plan_progress 结合使用：前者看节点级明细，后者看全局状态聚合。",
  "version": "1.0.0",
  "owner": "System",
  "beanName": "planNodeTools",
  "methodName": "listPhaseNodes",
  "inputSchema": {
    "type": "object",
    "properties": {
      "planId": {
        "type": "string",
        "description": "计划ID（必填）。用于限定查询范围到指定计划。"
      },
      "phaseId": {
        "type": "string",
        "description": "阶段ID（必填）。用于定位计划中的具体阶段。"
      }
    },
    "required": ["planId", "phaseId"]
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "status": { "type": "string" },
      "data": { "type": "array" }
    }
  }
}

{
  "name": "web_search",
  "description": "执行通用互联网网页搜索。当你需要获取最新资讯、实时数据、或者你自身知识库中缺乏的外部事实时调用此工具。输入精准的搜索关键词，返回相关的网页链接和摘要片段。",
  "version": "1.0.0",
  "owner": "System",
  "beanName": "searchTools",
  "methodName": "web_search",
  "inputSchema": {
    "type": "object",
    "properties": {
      "query": {
        "type": "string",
        "description": "搜索关键词（必填）。建议提供明确主题与限定词，例如“广州今天天气 实时”。"
      }
    },
    "required": ["query"]
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "organic": {
        "type": "array",
        "description": "网页搜索结果列表（通常包含标题、链接、摘要等字段）。"
      },
      "peopleAlsoAsk": {
        "type": "array",
        "description": "相关问题列表（可能为空）。"
      }
    }
  }
}

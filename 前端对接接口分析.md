# 前端对接接口分析

本文档基于当前项目源码整理，只包含前端可直接调用或前端需要感知的后端接口。文档不包含内部私有方法、工具方法、内部 Service 方法，也不对源码未明确的信息做猜测；凡源码无法确认之处，均明确标注“源码未明确说明”。

## 统一说明

### 统一鉴权机制

- 项目通过 `WebMvcConfig` 注册了全局 `AuthInterceptor`，默认拦截全部请求路径（Spring 路径模式为 /**，这里表示拦截范围，不是可调用接口）。
- 明确放行的路径只有：
- `/auth/login`
- `/api/luna/status/stream`
- Swagger / OpenAPI 文档相关路径
- 除上述放行路径外，其他接口默认都需要登录态。
- 鉴权请求头为 `Authorization`。
- 后端兼容两种传法：
- `Authorization: Bearer <token>`
- `Authorization: <token>`
- Token 为后端自行签发的 JWT。
- JWT 的 `jti` 会被后端当作当前登录会话的 `sessionId` 使用。
- JWT 默认有效期为 `86400` 秒。
- 当前源码未发现 `@PreAuthorize`、`@Secured`、角色注解或菜单级权限控制；当前权限要求基本只有“持有有效 JWT”这一层。

### 统一成功响应规律

- 项目没有统一的“成功响应包裹体”。
- 不同接口会直接返回不同结构：
- 简单对象，例如 `/auth/login` 返回 `{token}`。
- 业务对象或业务 JSON，例如聊天、审批、计划、MCP、Prompt 管理接口。
- 数组，例如历史记录接口返回 `List<String>`。
- 分页对象，查询类接口返回 `PagedResponse`。
- SSE 事件流，状态流接口不是一次性 JSON，而是持续推送事件。
- JSON-RPC 接口 `/mcp/rpc` 使用标准 JSON-RPC 包装结构，不同于普通 REST。

### 统一异常与错误返回

- 未登录、Token 缺失或 Token 非法时，拦截器直接返回 HTTP `401`：
- `status`：`String`，固定为 `unauthorized`
- `message`：`String`，常见为 `unauthorized` 或 `invalid token`
- 业务主动抛出 `AuthException` 时，也会返回 HTTP `401`，结构同样是：
- `status`：`String`，值为 `unauthorized`
- `message`：`String`，为具体错误信息
- 业务主动抛出 `NeedApprovalException` 时，会返回 HTTP `200`：
- `status`：`String`，值为 `pending_approval`
- `message`：`String`，值为“操作需要审批，请在前端确认”
- `taskId`：`String`，审批任务 ID
- 其他未捕获异常会进入 `GlobalExceptionHandler`，返回 HTTP `500`，响应体由 `ExceptionRetryService` 动态生成；源码可明确确认的常见字段有：
- `errorId`：`String`，错误追踪 ID
- `success`：`Boolean`，通常为 `false`
- `message`：`String`，错误说明或自动修复结果说明
- `reason`：`String`，原因说明
- `status`：`String`，部分自动修复场景可能返回 `pending_approval`
- `taskId`：`String`，自动修复触发审批时可能返回
- `repairResult`：`String`，自动修复成功时可能返回
- 需要特别注意：很多控制器没有单独把参数校验异常映射成 HTTP `400`，例如部分 Prompt/MCP 管理接口参数不合法时，实际可能直接进入全局异常处理并返回 HTTP `500`。

### SSE / 流式事件说明

- 当前项目只有一个明确对前端开放的流式接口：`/api/luna/status/stream`。
- 该接口底层使用 `SseSessionManager`，多个模块共用同一个默认客户端 `default`。
- 因此前端连上这一个 SSE 流后，不仅会收到普通状态事件，还可能收到审批事件、异步工作流事件、计划编排事件。
- 源码中已明确出现的事件名包括：
- `luna-status`
- `APPROVAL_REQUEST`
- `APPROVAL_RESULT`
- `WORKFLOW_ASYNC_RESULT`
- `SKILL_ASYNC_RESULT`
- `PLAN_CREATED`
- `PLAN_PHASE_STARTED`
- `PLAN_PHASE_FINISHED`
- `PLAN_FINISHED`
- `PLAN_REPORT_READY`
- `PLAN_FRONT_PROGRESS`
- `PLAN_PHASE_PROGRESS`
- `PLAN_CHECKPOINT_CREATED`
- `PLAN_NODE_RUNNING`
- `PLAN_NODE_SUCCESS`
- `PLAN_NODE_FAILED`
- `PLAN_REPLANNED`
- `PLAN_CODE_PATCH_READY`
- `PLAN_TEST_RESULT`
- `PLAN_BLUEPRINT_VALIDATED`
- `PLAN_BLUEPRINT_INVALID`
- 不同事件的 `data` 结构并不统一，前端需要按事件名区分解析。

## 认证接口

---

### 1. 接口名
用户登录

### 2. 方法类型
`POST`

### 3. 方法路径
`/auth/login`

### 4. 详细职责
该接口用于登录页提交账号密码并换取后续访问系统所需的 JWT。前端通常在登录按钮点击后调用，成功后把返回的 token 保存起来，并在后续受保护接口中放到 `Authorization` 请求头中。当前项目里，JWT 的 `jti` 会被后端当作后续业务会话的 `sessionId`，因此这个 token 不只是“登录凭证”，还会影响后续聊天、计划执行等链路的会话归属。

### 5. 是否鉴权
否。
该路径在 `WebMvcConfig` 中被显式放行，不需要先带 token。

### 6. 入参字段和类型
- 请求体为 JSON 对象。
- `username`：`String`，必填，登录用户名，默认值：无，取值范围：源码未明确说明。
- `password`：`String`，必填，登录密码，默认值：无，取值范围：源码未明确说明。

### 7. 回参字段和类型
- HTTP `200` 成功时返回 JSON 对象。
- `token`：`String`，JWT 令牌，成功时一定返回。
- HTTP `401` 失败时返回 JSON 对象。
- `status`：`String`，值为 `unauthorized`，失败时返回。
- `message`：`String`，通常为具体登录错误信息，例如用户名或密码错误，失败时返回。

---

### 1. 接口名
用户退出登录

### 2. 方法类型
`POST`

### 3. 方法路径
`/auth/logout`

### 4. 详细职责
该接口用于前端主动注销当前登录态。通常在用户点击退出登录时调用。后端会根据当前 token 提取 `jti` 并加入吊销集合，之后同一 token 再访问受保护接口会被视为未授权；因此前端在调用成功后，应同步清理本地保存的 token，并跳转回登录页或未登录态页面。

### 5. 是否鉴权
是。
需要 `Authorization` 请求头中的有效 JWT。当前源码未发现额外角色权限控制，仅要求 token 有效且未被吊销。

### 6. 入参字段和类型
- 请求头 `Authorization`：`String`，必填，登录 JWT，可带 `Bearer ` 前缀，也可直接传裸 token，默认值：无，取值范围：源码未明确说明。
- 请求体：无。

### 7. 回参字段和类型
- HTTP `200` 成功时返回 JSON 对象。
- `message`：`String`，退出成功提示，成功时一定返回。
- HTTP `401` 未授权时返回 JSON 对象。
- `status`：`String`，值为 `unauthorized`。
- `message`：`String`，值为 `unauthorized` 或 `invalid token`。

## 对话接口

---

### 1. 接口名
发送对话消息

### 2. 方法类型
`POST`

### 3. 方法路径
`/luna/api/chat/message`

### 4. 详细职责
这是前端聊天主入口。前端在用户发送一条新消息时调用，后端会在这一次请求里串起输入治理、上下文编译、记忆和知识检索、工具决策、主模型回复生成、记忆写回、状态事件推送等完整链路。对前端来说，这个接口不只是“拿回复”，还负责决定当前轮是否进入异步工作流、是否触发审批、以及本轮回复最终是什么格式；因此聊天页面的发送按钮、消息列表更新、审批弹窗、异步结果监听都和这个接口直接相关。

### 5. 是否鉴权
是。
需要 `Authorization` 请求头中的有效 JWT。后端优先使用 JWT 的 `jti` 作为本轮运行时 `sessionId`。当前源码未发现额外角色权限控制。

### 6. 入参字段和类型
- 请求体为 JSON 对象。
- `userInput`：`String`，必填，当前轮用户输入文本，后端会先执行 `trim()`；如果去空格后为空，会直接判定为无效输入，默认值：无，取值范围：源码未明确说明。

### 7. 回参字段和类型
- HTTP `200` 正常回复时，返回业务 JSON，对象结构不是全局统一包裹。
- `reply`：`String`，回复正文，正常主流程下应返回；源码显示后续链路依赖该字段。
- `emotion`：`String`，常见的情绪标签，源码中常见返回，但并非所有分支都能从控制层静态保证一定存在。
- `thought`：通常不会返回给前端；源码会在主流程中移除该字段。
- HTTP `200` 异步工作流挂起时，返回 JSON 对象。
- `emotion`：`String`，固定构造为 `Soft`，挂起分支通常返回。
- `reply`：`String`，提示后台任务正在执行，挂起分支通常返回。
- `status`：`String`，值为 `pending`，挂起分支返回。
- `taskId`：`String`，异步任务 ID，可能为空字符串。
- `workflowName`：`String`，当前后台工作流名称，可能退化为 `task`。
- HTTP `400` 入参为空时直接返回纯文本。
- 响应体：`String`，固定为 `empty input`。
- HTTP `503` 上下文治理阻断时返回 JSON 对象。
- `status`：`String`，值为 `context_governance_blocked`。
- `message`：`String`，阻断原因说明。
- HTTP `200` 审批中断场景可能由全局异常处理返回 JSON 对象。
- `status`：`String`，值为 `pending_approval`。
- `message`：`String`，审批提示。
- `taskId`：`String`，审批任务 ID。

---

### 1. 接口名
启动会话

### 2. 方法类型
`POST`

### 3. 方法路径
`/luna/api/chat/startup`

### 4. 详细职责
该接口用于前端在应用刚进入聊天页、桌面端刚启动或页面需要恢复欢迎语时主动拉起“开场回复”。后端会写入一条 `STARTUP` 启动消息，跑一套与普通聊天相同的受治理上下文流程，然后返回启动回复。前端通常在页面初始化完成后调用一次，用于展示开场白、恢复系统当前状态、或让用户在第一条手动消息前就看到 Luna 已上线。

### 5. 是否鉴权
是。
需要 `Authorization` 请求头中的有效 JWT。当前源码未发现额外角色权限控制。

### 6. 入参字段和类型
- 请求体：无。
- 查询参数：无。

### 7. 回参字段和类型
- HTTP `200` 成功时返回业务 JSON，结构与聊天主接口的正常回复基本一致。
- `reply`：`String`，启动回复正文，成功时通常返回。
- `emotion`：`String`，常见情绪标签，源码未严格保证一定返回。
- HTTP `503` 上下文治理阻断时返回 JSON 对象。
- `status`：`String`，值为 `context_governance_blocked`。
- `message`：`String`，常见为启动阶段治理后工作集为空的说明。

---

### 1. 接口名
关闭会话

### 2. 方法类型
`POST`

### 3. 方法路径
`/luna/api/chat/shutdown`

### 4. 详细职责
该接口用于前端在应用退出、聊天页关闭、或明确需要做会话收尾时通知后端执行关停逻辑。后端会记录关闭事件并写入对应的历史轨迹，但不会再生成一条聊天回复。因此前端通常在退出前静默调用，用来保证本次会话的收尾痕迹被保存，而不是用来刷新消息列表。

### 5. 是否鉴权
是。
需要 `Authorization` 请求头中的有效 JWT。当前源码未发现额外角色权限控制。

### 6. 入参字段和类型
- 请求体：无。
- 查询参数：无。

### 7. 回参字段和类型
- HTTP `200` 成功时无响应体。
- 前端应按“调用成功即可”处理，不要期待 JSON 数据。

---

### 1. 接口名
查询有历史记录的日期列表

### 2. 方法类型
`GET`

### 3. 方法路径
`/luna/api/chat/history/date`

### 4. 详细职责
该接口用于历史聊天面板先按“月”取出有哪些“天”存在记录。典型用法是前端先让用户选择某个月，再调用本接口拿到该月下存在聊天记录的日期列表，用于渲染日历高亮、日期列表、历史入口等。它本身不返回聊天正文，只负责告诉前端“哪些日期有历史可以点进去看”。

### 5. 是否鉴权
是。
需要 `Authorization` 请求头中的有效 JWT。当前源码未发现额外角色权限控制。

### 6. 入参字段和类型
- 查询参数 `ym`：`String`，必填，年月前缀；源码按照 `yyyy:MM` 形式使用，例如 `2026:04`，默认值：无，取值范围：源码未做更严格校验。

### 7. 回参字段和类型
- HTTP `200` 成功时返回 `Array<String>`。
- 数组每一项：`String`，表示该月下存在历史记录的“日”部分，例如 `12`。
- 当 `ym` 格式不满足源码预期时，可能直接返回空数组。

---

### 1. 接口名
查询指定日期的聊天历史

### 2. 方法类型
`GET`

### 3. 方法路径
`/luna/api/chat/history`

### 4. 详细职责
该接口用于前端在用户点开某一天后，拉取该日完整聊天回放。前端通常会先通过“查询有历史记录的日期列表”拿到可选日期，再用具体日期调用本接口，把返回结果渲染到聊天记录回放区域。该接口直接返回字符串数组，不做额外对象包装，所以前端需要自行拆分每一项中的角色、文本和时间。

### 5. 是否鉴权
是。
需要 `Authorization` 请求头中的有效 JWT。当前源码未发现额外角色权限控制。

### 6. 入参字段和类型
- 查询参数 `ymd`：`String`，必填，目标日期键；源码按 `yyyy:MM:dd` 形式使用，例如 `2026:04:12`，默认值：无，取值范围：源码未做更严格校验。

### 7. 回参字段和类型
- HTTP `200` 成功时返回 `Array<String>`。
- 数组每一项：`String`，格式为 `ROLE:content:time`。
- `ROLE`：`String`，消息角色，例如 `USER`、`LUNA`、`STARTUP`。
- `content`：`String`，该条消息正文。
- `time`：`String`，消息时间文本，具体格式取决于 `ChatMessage.getTime()` 的序列化结果。

## 审批接口

---

### 1. 接口名
提交审批结果

### 2. 方法类型
`POST`

### 3. 方法路径
`/mcp/tools/approval`

### 4. 详细职责
当前端收到待审批工具调用后，需要通过这个接口把“用户同意”或“用户拒绝”的结果回传给后端。后端收到后会继续推进原来的业务链路：如果审批通过，就执行目标工具并继续恢复聊天；如果审批拒绝，就跳过工具执行，继续生成一条拒绝后的后续回复。对前端来说，这个接口通常配合 SSE 中的 `APPROVAL_REQUEST` 事件一起使用，用于审批弹窗点击确认或拒绝后的提交。

### 5. 是否鉴权
是。
需要 `Authorization` 请求头中的有效 JWT。当前源码未发现额外角色权限控制。

### 6. 入参字段和类型
- 请求体为 JSON 对象。
- `taskId`：`String`，必填，审批任务 ID，默认值：无，取值范围：源码未明确说明。
- `approved`：`Boolean` 或 `Number` 或 `String`，必填，审批结果。
- `approved=true`、`1`、`yes` 会被解析为同意。
- `approved=false`、`0`、`no` 会被解析为拒绝。
- 其他取值会被视为无效参数。

### 7. 回参字段和类型
- HTTP `400` 参数缺失或无法识别时返回 JSON 对象。
- `status`：`String`，值为 `error`。
- `message`：`String`，固定为 `taskId and approved are required`。
- HTTP `200` 成功处理审批后，返回值会尽量解析成 JSON；返回结构取决于审批结果和后续恢复链路。
- 常见成功分支会返回聊天回复对象。
- `reply`：`String`，审批后续聊回复或工具完成提示，常见返回。
- `emotion`：`String`，情绪标签，常见返回。
- 常见失败分支会返回错误对象。
- `status`：`String`，值为 `error`。
- `message`：`String`，错误信息。
- `emotion`：`String`，常见为 `Solemn`。
- `reply`：`String`，面向用户的失败提示。
- 如果服务层返回的不是合法 JSON，控制层会兜底返回：
- `raw`：`String`，原始字符串结果。

---

### 1. 接口名
提交旧版审批结果兼容入口

### 2. 方法类型
`POST`

### 3. 方法路径
`/mcp/skills/approval`

### 4. 详细职责
这是历史路径兼容接口，作用是保证旧前端或旧调用方在迁移期间仍然可以提交审批结果。它不会执行独立业务逻辑，而是把请求体原样转发到新版 `/mcp/tools/approval` 审批接口。因此前端只有在兼容历史版本、或者确认线上环境开启了兼容开关时才需要继续调用它；新对接应优先使用新版路径。

### 5. 是否鉴权
是。
需要 `Authorization` 请求头中的有效 JWT。该接口只有在配置 `luna.mcp.compat.skill-endpoint-enabled=true` 时才会生效。当前源码未发现额外角色权限控制。

### 6. 入参字段和类型
- 请求体为 JSON 对象，字段与新版审批接口完全一致。
- `taskId`：`String`，必填，审批任务 ID。
- `approved`：`Boolean` 或 `Number` 或 `String`，必填，审批结论。

### 7. 回参字段和类型
- 回参与新版 `/mcp/tools/approval` 完全一致。
- 常见分支包括参数错误对象、审批后的聊天回复对象、失败对象或 `raw` 原始结果对象。

## 状态流接口

---

### 1. 接口名
订阅 Luna 状态流

### 2. 方法类型
`GET`

### 3. 方法路径
`/api/luna/status/stream`

### 4. 详细职责
该接口用于前端建立一个长期存在的 SSE 连接，持续接收 Luna 的运行状态、审批通知、异步工作流结果以及计划编排事件。前端通常在聊天页、任务执行页或桌面应用主界面初始化后立即调用一次，并保持连接不断开；之后用户发送消息、触发审批、运行工作流、执行计划时，界面上的状态条、审批弹窗、进度条、异步结果提示都应该主要依赖这条流来实时刷新。源码中连接建立后会立刻主动推送一个初始 `IDLE` 状态，方便前端拿到稳定起始值。

### 5. 是否鉴权
否。
该路径在 `WebMvcConfig` 中被显式放行，不需要 `Authorization` 请求头。

### 6. 入参字段和类型
- 请求体：无。
- 查询参数：无。
- 建议前端使用 `EventSource` 或兼容的 SSE 客户端调用。
- 返回头部会被后端设置为 `text/event-stream`、`Cache-Control: no-cache`、`Connection: keep-alive`。

### 7. 回参字段和类型
- 该接口不是一次性 JSON，而是持续推送 SSE 事件。
- 初始事件和普通状态事件常见事件名为 `luna-status`，对应数据结构为 `LunaStatusMessage`。
- `eventType`：`String`，事件类型，常见值为 `luna-status`。
- `status`：`String`，状态码，源码中已明确出现过 `IDLE`、`THINKING`、`RETRIEVING`、`STARTING`、`PLAN`、`WORKING`、`SEARCHING`、`SCRAPING`、`MEMORY`、`SCHEDULE`、`KNOWLEDGE_BASE`、`LOG`、`REPORT`、`LOCK`、`DESKTOP`、`CODEOPS` 等。
- `message`：`String`，前端展示文案。
- `traceId`：`String`，事件链路 ID，可为空。
- `taskId`：`String`，任务 ID，可为空。
- `timestamp`：`Long`，毫秒时间戳。
- 审批请求事件名为 `APPROVAL_REQUEST`，数据结构为 `ApprovalTask`。
- `taskId`：`String`，审批任务 ID。
- `sessionId`：`String`，原会话 ID。
- `resourceId`：`Long`，审批资源主键。
- `status`：`String`，审批状态，常见为 `PENDING_APPROVAL`。
- `skillName`：`String`，展示给前端的能力名。
- `serverCode`：`String`，MCP 服务编码。
- `toolName`：`String`，工具名称。
- `argsJson`：`String`，工具入参 JSON。
- `result`：`String`，审批结束后结果，审批请求阶段通常为空。
- `errorCode`：`String`，错误码，请求阶段通常为空。
- `createTime`：`Long`，创建时间戳。
- `updateTime`：`Long`，更新时间戳。
- `chatSessionKey`：`String`，恢复聊天用的会话键。
- `userInput`：`String`，触发审批的原始用户输入。
- `memorySnippets`：`Array<String>`，审批前上下文记忆。
- `knowledgeSnippets`：`Array<String>`，审批前知识片段。
- `preferenceSnippets`：`Array<String>`，审批前偏好片段。
- `longTermMemorySnippets`：`Array<String>`，审批前长期记忆片段。
- 审批结果事件名为 `APPROVAL_RESULT`。
- `taskId`：`String`，审批任务 ID。
- `approved`：`Boolean`，是否同意。
- `result`：`Object` 或 `String`，审批后恢复链路的结果；如果原始结果可解析为 JSON，则这里是对象，否则是字符串。
- 异步技能/工作流结果事件名为 `WORKFLOW_ASYNC_RESULT` 和兼容旧名 `SKILL_ASYNC_RESULT`，两者 payload 相同。
- `eventType`：`String`，值为 `WORKFLOW_ASYNC_RESULT`。
- `taskId`：`String`，异步任务 ID。
- `workflowName`：`String`，工作流名称。
- `skillName`：`String`，兼容旧字段，通常与 `workflowName` 相同。
- `planId`：`String`，源码当前固定填空字符串。
- `phaseId`：`String`，源码当前固定填空字符串。
- `nodeId`：`String`，源码当前固定填空字符串。
- `status`：`String`，取值见源码，常见 `COMPLETED` 或 `FAILED`。
- `success`：`Boolean`，是否成功。
- `message`：`String`，异步执行结果说明。
- `errorCode`：`String`，失败错误码，成功时常为空字符串。
- `error`：`String`，失败说明，成功时可能为空。
- `result`：`String`，异步任务原始结果文本，失败时可能为空。
- `costMs`：`Long`，执行耗时毫秒。
- `retryCount`：`Integer`，源码当前固定为 `0`。
- `timestamp`：`Long`，毫秒时间戳。
- 计划前端进度事件名 `PLAN_FRONT_PROGRESS`。
- `planId`：`String`，计划 ID。
- `stage`：`String`，进度阶段，例如 `PLAN_CREATED`、`PLAN_RUNNING`、`REPORT_READY`、`REPORT_FAILED`、`PHASE_SUCCESS` 等。
- `message`：`String`，阶段说明。
- `phaseTotal`：`Integer`，阶段总数。
- `phaseFinished`：`Integer`，已完成阶段数。
- `nodeSuccess`：`Long`，成功节点数。
- `nodeFailed`：`Long`，失败节点数。
- `progressPercent`：`Integer`，按阶段进度计算的百分比。
- `timestamp`：`Long`，毫秒时间戳。
- 计划阶段进度事件名 `PLAN_PHASE_PROGRESS`。
- `planId`：`String`，计划 ID。
- `phaseId`：`String`，阶段 ID。
- `phaseOrder`：`Integer`，阶段顺序。
- `phaseName`：`String`，阶段名。
- `phaseStatus`：`String`，阶段状态。
- `message`：`String`，阶段说明。
- `timestamp`：`Long`，毫秒时间戳。
- 计划普通事件名还包括 `PLAN_CREATED`、`PLAN_PHASE_STARTED`、`PLAN_PHASE_FINISHED`、`PLAN_REPORT_READY`、`PLAN_FINISHED` 等。
- 这类事件的 payload 至少会包含 `planId`、`status`、`message`、`timestamp`，部分事件还会附带 `sessionId`、`userGoal`、`effectiveGoal`、`phaseId`、`phaseOrder`、`reportPath`、`reportUrl`、`openResult`、`planVersion` 等字段。
- 计划节点类事件、检查点类事件、蓝图事件的结构会随事件类型变化；前端应始终按 `event` 名称分支解析，不能假设所有事件都共享同一字段集合。

---

### 1. 接口名
断开状态流

### 2. 方法类型
`GET`

### 3. 方法路径
`/api/luna/status/disconnect`

### 4. 详细职责
该接口用于前端主动关闭默认客户端的 SSE 连接。适合在明确退出当前会话页面、切换账号、或想让服务端立刻释放默认连接时调用。它不会返回事件摘要，也不会回放未读事件，只是执行一次服务端断连动作并返回一段纯文本确认。

### 5. 是否鉴权
是。
虽然状态流订阅接口本身被放行，但这个断开接口并没有被排除，因此仍然需要 `Authorization` 请求头中的有效 JWT。当前源码未发现额外角色权限控制。

### 6. 入参字段和类型
- 请求体：无。
- 查询参数：无。

### 7. 回参字段和类型
- HTTP `200` 成功时返回纯文本。
- 响应体：`String`，固定为 `已断开连接`。

## 计划编排接口

---

### 1. 接口名
创建并执行计划

### 2. 方法类型
`POST`

### 3. 方法路径
`/luna/api/plan/run`

### 4. 详细职责
该接口用于让前端把一个用户目标交给后端，直接生成计划蓝图并开始执行。典型场景是用户在“任务规划”“自动执行”“复杂操作代理”类页面提交一个目标后调用它，后端会创建计划实例、生成阶段和节点、依次执行阶段、生成最终报告，并把结果整体汇总返回。前端调用后不仅能拿到最终计划结果，还应配合状态流监听同一计划的实时进度，因为计划执行过程本身会持续产生 `PLAN_*` 系列事件。

### 5. 是否鉴权
是。
需要 `Authorization` 请求头中的有效 JWT。源码中该接口会优先使用 JWT 的 `jti` 作为 `sessionId`；只有当登录态里拿不到 `jti` 时，才会退回请求体中的 `sessionId`。当前源码未发现额外角色权限控制。

### 6. 入参字段和类型
- 请求体为 JSON 对象。
- `sessionId`：`String`，非必填，计划归属会话 ID；如果当前请求已带有效 JWT，后端会优先使用 JWT `jti`，默认值：无。
- `userGoal`：`String`，必填，用户目标描述，默认值：无，去空格后为空会被判定为非法请求。

### 7. 回参字段和类型
- HTTP `200` 成功时返回 JSON 对象。
- `planId`：`String`，计划 ID，成功时一定返回。
- `phaseResults`：`Array<Object>`，阶段执行结果列表，成功时返回。
- `phaseResults[].phaseId`：`String`，阶段 ID。
- `phaseResults[].phaseOrder`：`Number`，阶段顺序。
- `phaseResults[].result`：`Object` 或 `String`，该阶段原始执行结果；控制层会尽量解析成 JSON，解析失败时保留原样。
- `reportResult`：`Object` 或 `String`，计划收尾和报告生成结果。
- `status`：`String`，最终汇总状态，源码中常见 `success` 或 `error`。
- `message`：`String`，执行结果说明。
- HTTP `400` 参数错误时返回 JSON 对象。
- `status`：`String`，值为 `error`。
- `errorCode`：`String`，值为 `INVALID_REQUEST`。
- `message`：`String`，具体错误信息。
- HTTP `500` 控制层异常时返回 JSON 对象。
- `status`：`String`，值为 `error`。
- `errorCode`：`String`，值为 `PLAN_RUN_FAILED`。
- `message`：`String`，异常信息或默认文案。

---

### 1. 接口名
执行计划阶段

### 2. 方法类型
`POST`

### 3. 方法路径
`/luna/api/plan/phase/run`

### 4. 详细职责
该接口用于前端手动推进某一个已存在计划中的指定阶段，而不是重新跑整个计划。适合做“分阶段执行”“失败后只重跑某阶段”“人工审核后继续某阶段”的交互。前端调用时需要已经拿到 `planId` 和 `phaseId`，通常来自计划图谱、计划详情页或者状态流中的阶段事件。

### 5. 是否鉴权
是。
需要 `Authorization` 请求头中的有效 JWT。当前源码未发现额外角色权限控制。

### 6. 入参字段和类型
- 请求体为 JSON 对象。
- `planId`：`String`，必填，目标计划 ID，默认值：无。
- `phaseId`：`String`，必填，目标阶段 ID，默认值：无。

### 7. 回参字段和类型
- HTTP `200` 成功时返回该阶段的原始执行结果；控制层会优先解析成 JSON，无法解析时包装为成功对象。
- 解析成功时：返回 `Object`，字段取决于具体阶段执行器。
- 解析失败时：返回 JSON 对象。
- `status`：`String`，值为 `success`。
- `raw`：`String`，阶段执行返回的原始文本。
- HTTP `400` 参数错误时返回 JSON 对象。
- `status`：`String`，值为 `error`。
- `errorCode`：`String`，值为 `INVALID_REQUEST`。
- `message`：`String`，具体错误说明。
- HTTP `500` 控制层异常时返回 JSON 对象。
- `status`：`String`，值为 `error`。
- `errorCode`：`String`，值为 `PLAN_PHASE_RUN_FAILED`。
- `message`：`String`，异常信息或默认文案。

---

### 1. 接口名
生成计划最终报告

### 2. 方法类型
`POST`

### 3. 方法路径
`/luna/api/plan/report/finalize`

### 4. 详细职责
该接口用于在计划节点执行完成后，汇总当前计划的阶段、节点和最终状态，生成 HTML 报告并返回报告访问结果。前端通常在“计划执行完成后点生成报告”“计划详情页主动补生成报告”“失败任务也要出复盘报告”这类场景中调用。调用后前端可以直接拿到报告路径、报告 URL 和最终统计信息，并据此展示下载入口、打开入口或执行结果总览。

### 5. 是否鉴权
是。
需要 `Authorization` 请求头中的有效 JWT。当前源码未发现额外角色权限控制。

### 6. 入参字段和类型
- 请求体为 JSON 对象。
- `planId`：`String`，必填，目标计划 ID，默认值：无。

### 7. 回参字段和类型
- HTTP `200` 成功时返回 JSON 对象。
- `status`：`String`，值为 `success`。
- `planId`：`String`，计划 ID。
- `finalStatus`：`String`，最终状态，源码明确可能为 `SUCCESS`、`FAILED`、`PARTIAL`。
- `reportPath`：`String`，报告文件本地路径。
- `reportUrl`：`String`，报告访问 URL。
- `openResult`：`String`，尝试打开报告文件的结果标记。
- `nodeTotal`：`Number`，节点总数。
- `nodeSuccess`：`Number`，成功节点数。
- `nodeFailed`：`Number`，失败节点数。
- `nodeSkipped`：`Number`，跳过节点数。
- HTTP `400` 参数错误时返回 JSON 对象。
- `status`：`String`，值为 `error`。
- `errorCode`：`String`，值为 `INVALID_REQUEST`。
- `message`：`String`，具体错误说明。
- HTTP `500` 控制层异常时返回 JSON 对象。
- `status`：`String`，值为 `error`。
- `errorCode`：`String`，值为 `PLAN_FINALIZE_FAILED`。
- `message`：`String`，异常信息或默认文案。

---

### 1. 接口名
查询计划图谱

### 2. 方法类型
`GET`

### 3. 方法路径
`/luna/api/plan/graph/{planId}`

### 4. 详细职责
该接口用于前端拿到某个计划的完整图结构快照，包括计划基本信息、阶段列表、节点列表、边关系以及节点状态统计。它适合被计划详情页、任务看板、流程图视图、失败排查页面调用。前端调用后可以直接把结果渲染成计划流程图、阶段时间线、节点状态表、风险概览等可视化组件，而不需要再额外拼装多个接口。

### 5. 是否鉴权
是。
需要 `Authorization` 请求头中的有效 JWT。当前源码未发现额外角色权限控制。

### 6. 入参字段和类型
- 路径参数 `planId`：`String`，必填，目标计划 ID，默认值：无。

### 7. 回参字段和类型
- HTTP `200` 成功时返回 JSON 对象。
- `planId`：`String`，计划 ID。
- `sessionId`：`String`，计划所属会话 ID。
- `userGoal`：`String`，用户目标。
- `status`：`String`，计划状态，源码明确可能为 `PENDING`、`RUNNING`、`WAITING_USER_APPROVAL`、`SUCCESS`、`FAILED`、`CANCELLED`。
- `finalStatus`：`String`，计划最终状态，源码明确可能为 `SUCCESS`、`FAILED`、`PARTIAL`、`CANCELLED`。
- `planVersion`：`Number`，计划版本号。
- `phases`：`Array<Object>`，阶段列表。
- `phases[].phaseId`：`String`，阶段 ID。
- `phases[].phaseOrder`：`Number`，阶段顺序。
- `phases[].name`：`String`，阶段名称。
- `phases[].objective`：`String`，阶段目标。
- `phases[].status`：`String`，阶段状态，源码明确可能为 `PENDING`、`RUNNING`、`SUCCESS`、`FAILED`。
- `nodes`：`Array<Object>`，节点列表。
- `nodes[].nodeId`：`String`，节点 ID。
- `nodes[].phaseId`：`String`，所属阶段 ID。
- `nodes[].name`：`String`，节点名称。
- `nodes[].nodeType`：`String`，节点类型，源码明确可能为 `ANALYZE`、`TOOL`、`VALIDATE`、`REPORT`、`CODE`、`PROMPT`、`RESOURCE`、`WORKFLOW`。
- `nodes[].status`：`String`，节点状态，源码明确可能为 `PENDING`、`RUNNING`、`SUCCESS`、`FAILED`、`BLOCKED`、`APPROVAL_PENDING`、`SKIPPED`。
- `nodes[].riskLevel`：`String`，风险等级，源码明确可能为 `LOW`、`MEDIUM`、`HIGH`。
- `nodes[].retryCount`：`Number`，已重试次数。
- `nodes[].maxRetry`：`Number`，最大重试次数。
- `nodes[].costMs`：`Number`，执行耗时毫秒。
- `nodes[].failReason`：`String`，失败原因，没有时为空字符串。
- `nodes[].dependencies`：`Array<String>` 或 `Array<Object>`，节点依赖集合，源码未进一步限定元素结构。
- `nodes[].outputForNext`：`Object`，传给后续节点的输出数据，没有时为空对象。
- `edges`：`Array<Object>`，边列表。
- `edges[].fromNodeId`：`String`，起点节点 ID。
- `edges[].toNodeId`：`String`，终点节点 ID。
- `edges[].conditionExpr`：`String`，条件表达式，没有时为空字符串。
- `nodeStats`：`Object`，节点状态统计映射，键为节点状态值，值为对应数量。
- HTTP `400` 参数错误时返回 JSON 对象。
- `status`：`String`，值为 `error`。
- `errorCode`：`String`，值为 `INVALID_REQUEST`。
- `message`：`String`，具体错误说明。
- HTTP `500` 控制层异常时返回 JSON 对象。
- `status`：`String`，值为 `error`。
- `errorCode`：`String`，值为 `PLAN_GRAPH_FAILED`。
- `message`：`String`，异常信息或默认文案。

## RAG 检索接口

---

### 1. 接口名
执行受治理的 RAG 检索

### 2. 方法类型
`POST`

### 3. 方法路径
`/luna/api/rag/retrieve`

### 4. 详细职责
该接口用于让前端在需要“查知识、查记忆、查偏好”时发起一次受治理的检索请求。它和普通搜索不同，后端不会直接拿原始 query 去检索，而是先做输入治理、上下文重建和节点工作集决策，再生成一个更适合检索的 governed query，再真正进入检索管线。前端通常在知识问答、辅助回答、证据查看、调试检索结果页面调用它；如果你需要显示命中来源、改写 query、证据分组、耗时和调试信息，这个接口就是对应入口。

### 5. 是否鉴权
是。
需要 `Authorization` 请求头中的有效 JWT。当前源码未发现额外角色权限控制。

### 6. 入参字段和类型
- 请求体为 JSON 对象。
- `query`：`String`，必填，原始检索问题，默认值：无；去空格后为空会直接返回 HTTP `400`。
- `sessionId`：`String`，非必填，会话 ID；为空时后端会默认补成 `rag-governed-session`。
- `conversationContext`：`Array<Object>`，非必填，对话上下文消息列表，默认值为空数组。
- `conversationContext[].role`：`String`，消息角色，例如 `user`、`assistant`、`system`。
- `conversationContext[].content`：`String`，消息内容。
- `allowedRoutes`：`Array<String>`，非必填，允许参与本次检索的路由集合；为空时默认允许全部路由。
- `allowedRoutes[]`：`String`，可选值为 `search`、`native`、`modular`、`agentic`。
- `sourceScope`：`Array<String>`，非必填，允许参与检索的数据源集合；为空时默认允许全部数据源。
- `sourceScope[]`：`String`，可选值为 `knowledge`、`memory`、`preference`。
- `options`：`Object`，非必填，检索附加选项。
- `options.debug`：`Boolean`，非必填，是否返回更多调试信息，默认值 `false`。
- `options.maxLatencyMs`：`Long`，非必填，检索最大延迟预算毫秒，默认值 `2500`。

### 7. 回参字段和类型
- HTTP `200` 成功时返回 `RetrievalResponse`。
- `route`：`String`，最终命中的检索路由，可能为 `search`、`native`、`modular`、`agentic`。
- `rewrittenQuery`：`String`，检索阶段真正使用的改写 query。
- `evidences`：`Object`，按数据源分组的证据集合。
- `evidences.knowledge`：`Array<Object>`，知识库证据列表，是否返回取决于是否命中。
- `evidences.memory`：`Array<Object>`，记忆证据列表，是否返回取决于是否命中。
- `evidences.preference`：`Array<Object>`，偏好证据列表，是否返回取决于是否命中。
- `evidences.*[].id`：`String`，证据 ID。
- `evidences.*[].source`：`String`，证据来源，可能为 `knowledge`、`memory`、`preference`。
- `evidences.*[].type`：`String`，证据类别字符串。
- `evidences.*[].role`：`String`，语义角色，可能为 `fact`、`experience`、`preference`、`strategy`。
- `evidences.*[].title`：`String`，证据标题，可为空。
- `evidences.*[].content`：`String`，证据正文。
- `evidences.*[].score`：`Number`，排序分数。
- `evidences.*[].metadata`：`Object`，来源扩展元数据。
- `evidenceRoleGroups`：`Object`，按语义角色分组的证据集合，键可能为 `fact`、`experience`、`preference`、`strategy`。
- `meta`：`Object`，检索与治理元数据。
- `meta.governed`：`Boolean`，是否经过治理，当前控制器会补成 `true`。
- `meta.rawQuery`：`String`，原始 query。
- `meta.governedQuery`：`String`，治理后的 query。
- `meta.mcpDrivenInput`：`String`，节点工作集阶段生成的 MCP 驱动输入，可能为空字符串。
- `meta.sources_used`：`Object` 或 `Array`，实际使用的数据源，源码未进一步固定结构。
- `meta.latency_ms`：`Number`，检索耗时毫秒。
- `meta.query_type`：`String`，检索 query 类型。
- `meta.session_id`：`String`，会话 ID。
- `meta.needs_rerank`：`Boolean`，是否需要重排。
- `meta.source_routing`：`Object`，模块化路由下可能返回。
- `meta.rewrite_applied`：`Boolean`，是否做了 query 改写，部分路由可能返回。
- `meta.evidence_role_grouping`：`Object`，角色分组相关元数据，部分路由可能返回。
- `meta` 下还可能附带更多路由级调试字段；源码未统一限定键集合，前端应按“可选透传展示”处理。
- HTTP `400` 当 `query` 为空时，控制层直接返回空响应体。
- HTTP `422` 当治理后 query 为空时，返回 `RetrievalResponse`，其中：
- `meta.governed`：`Boolean`，值为 `true`。
- `meta.status`：`String`，值为 `rejected`。
- `meta.reason`：`String`，固定说明治理失败。
- `meta.rawQuery`：`String`，原始 query。
- `meta.governedQuery`：`String`，通常为空字符串。
- `meta.mcpDrivenInput`：`String`，治理阶段的 MCP 输入。

## MCP 接口

---

### 1. 接口名
旧版工具写接口（已退役）

### 2. 方法类型
`POST`
`PUT`
`DELETE`

### 3. 方法路径
`/mcp/tools`
`/mcp/tools`
`/mcp/tools/{id}`

### 4. 详细职责
这组接口是历史遗留的 MCP 工具写入入口，源码中已经明确退役，不再真正执行注册、更新或删除。前端如果还保留旧版能力中心或历史管理页面，调用这些路径只会收到“接口已废弃”的提示，不会对数据库或目录产生任何修改。新对接应改用新的迁移写入接口，例如 `/mcp/migrate/tool-catalog` 和相关能力目录路径。

### 5. 是否鉴权
是。
需要 `Authorization` 请求头中的有效 JWT。当前源码未发现额外角色权限控制。

### 6. 入参字段和类型
- `POST /mcp/tools`：无请求体要求，源码未读取请求体。
- `PUT /mcp/tools`：无请求体要求，源码未读取请求体。
- `DELETE /mcp/tools/{id}`：路径参数 `id` 为 `Long`，理论上必填，但当前退役逻辑不会真正使用该值。

### 7. 回参字段和类型
- 统一返回 HTTP `410`。
- `code`：`Number`，固定为 `410`。
- `message`：`String`，提示旧版 `/mcp/tools` 写接口已退役，并给出替代路径说明。

---

### 1. 接口名
旧版技能写接口（已退役）

### 2. 方法类型
`POST`
`PUT`
`DELETE`

### 3. 方法路径
`/mcp/skills`
`/mcp/skills`
`/mcp/skills/{id}`

### 4. 详细职责
这组接口是历史技能写入入口，和旧版工具写接口一样，当前只保留作兼容提示，不再承担真实写入功能。前端如果还存在旧版技能后台或历史脚本，调用时只能得到“已退役”的结果。新能力接入应该改用工作流模板、提示词目录等新的 MCP 迁移接口。

### 5. 是否鉴权
是。
需要 `Authorization` 请求头中的有效 JWT。当前源码未发现额外角色权限控制。

### 6. 入参字段和类型
- `POST /mcp/skills`：无请求体要求，源码未读取请求体。
- `PUT /mcp/skills`：无请求体要求，源码未读取请求体。
- `DELETE /mcp/skills/{id}`：路径参数 `id` 为 `Long`，理论上必填，但当前退役逻辑不会真正使用该值。

### 7. 回参字段和类型
- 统一返回 HTTP `410`。
- `code`：`Number`，固定为 `410`。
- `message`：`String`，提示旧版 `/mcp/skills` 写接口已退役，并给出替代路径说明。

---

### 1. 接口名
查询全部能力资源

### 2. 方法类型
`GET`

### 3. 方法路径
`/mcp/resources`

### 4. 详细职责
该接口用于前端一次性拉取当前系统已知的所有 MCP 能力资源，包括工具、提示词、资源、工作流，以及源码里额外补充的本地域资源模板。适合在能力市场、能力发现页、调试面板、管理员能力总览页初始化时调用。因为返回结果已经被统一转换成 `Resource` 视图，所以前端不需要自己区分多张目录表，可以直接按 `type` 分类渲染。

### 5. 是否鉴权
是。
需要 `Authorization` 请求头中的有效 JWT。当前源码未发现额外角色权限控制。

### 6. 入参字段和类型
- 请求体：无。
- 查询参数：无。

### 7. 回参字段和类型
- HTTP `200` 成功时返回 `Array<Resource>`。
- `id`：`String`，统一资源 ID。
- `type`：`String`，资源类型，可能为 `TOOL`、`PROMPT`、`RESOURCE`、`WORKFLOW`、`STRATEGY`。
- `name`：`String`，资源名称。
- `serverCode`：`String`，资源所属服务编码。
- `description`：`String`，用途描述。
- `resourceUri`：`String`，资源 URI，仅资源类常见。
- `mimeType`：`String`，MIME 类型，仅资源类常见。
- `version`：`String`，版本号。
- `executionMode`：`String`，执行模式文本，例如 `MCP`、`LEGACY`；部分资源可能为空。
- `owner`：`String`，所有者，源码未明确说明是否一定返回。
- `inputSchema`：`String`，输入 Schema 的 JSON 字符串，工具/工作流常见。
- `outputSchema`：`String`，输出 Schema 的 JSON 字符串，工具/工作流常见。
- `argumentsSchema`：`String`，参数 Schema 的 JSON 字符串，提示词常见。
- `runMode`：`String`，运行模式，可能为 `SYNC`、`ASYNC`。
- `requiresApproval`：`Boolean`，是否需要审批。
- `sensitivity`：`String`，敏感等级，可能为 `LOW`、`MEDIUM`、`HIGH`。
- `requiredCapabilities`：`Array<String>`，工作流依赖能力列表，按类型返回。
- `toolSlots`：`Array<Object>`，工作流插槽列表，按类型返回。
- `toolSlots[].slot`：`String`，插槽名。
- `toolSlots[].capability`：`String`，能力要求。
- `toolSlots[].required`：`Boolean`，是否必填。
- `thoughtChain`：`Array<String>`，工作流思维链或提示列表，按类型返回。

---

### 1. 接口名
查询单个能力资源详情

### 2. 方法类型
`GET`

### 3. 方法路径
`/mcp/resources/{id}`

### 4. 详细职责
该接口用于根据目录主键查看单个能力的详细信息。前端通常在能力详情弹窗、资源查看页、能力配置页点击某条能力后调用。需要注意的是，这里只按数据库中的数值主键查询工具、提示词、资源、工作流目录记录；源码里那些额外拼装的“本地域资源模板”并不能通过这个接口按字符串 ID 反查。

### 5. 是否鉴权
是。
需要 `Authorization` 请求头中的有效 JWT。当前源码未发现额外角色权限控制。

### 6. 入参字段和类型
- 路径参数 `id`：`Long`，必填，资源数据库主键。

### 7. 回参字段和类型
- HTTP `200` 成功时返回 `Resource` 对象，字段与“查询全部能力资源”接口中的单条资源字段相同。
- 如果未命中任何记录，控制层仍返回 HTTP `200`，响应体可能为 `null`。

---

### 1. 接口名
搜索能力资源

### 2. 方法类型
`POST`

### 3. 方法路径
`/mcp/search`

### 4. 详细职责
该接口用于让前端根据关键词做能力发现。后端会先对全部资源做关键词匹配，再叠加语义检索结果，并按资源唯一键去重后返回。前端适合在能力搜索框输入、工具选择器、工作流配置时的“搜索能力”场景调用；如果只是想拿全量目录，用 `/mcp/resources` 更合适，如果想按某个服务分类查看，则应使用工具/提示词/资源的 list 接口。

### 5. 是否鉴权
是。
需要 `Authorization` 请求头中的有效 JWT。当前源码未发现额外角色权限控制。

### 6. 入参字段和类型
- 请求体为 JSON 对象。
- `query`：`String`，非必填，但为空白时后端会直接返回空数组，建议前端按必填处理。

### 7. 回参字段和类型
- HTTP `200` 成功时返回 `Array<Resource>`。
- 单条资源字段与“查询全部能力资源”接口一致。
- 当 `query` 为空或只包含空白字符时，返回空数组。

---

### 1. 接口名
查询 MCP 工具目录

### 2. 方法类型
`GET`

### 3. 方法路径
`/mcp/tools/list`

### 4. 详细职责
该接口用于前端查看某个 MCP 服务下有哪些工具可用，或者在未指定服务时查看全部工具。它适合被工具选择器、调试控制台、审批前信息展示、工具目录页调用。和 `/mcp/resources` 相比，这个接口只返回工具，不返回提示词、资源、工作流，而且返回的是工具描述 DTO，字段更贴近“可调用工具”的视角。

### 5. 是否鉴权
是。
需要 `Authorization` 请求头中的有效 JWT。当前源码未发现额外角色权限控制。

### 6. 入参字段和类型
- 查询参数 `serverCode`：`String`，非必填，目标服务编码；为空时底层会优先路由到本地服务 `local-agent-server`，或者按适配器逻辑返回全量可见工具。

### 7. 回参字段和类型
- HTTP `200` 成功时返回 `Array<McpToolDescriptor>`。
- `serverCode`：`String`，服务编码。
- `toolName`：`String`，工具名称。
- `title`：`String`，展示标题。
- `description`：`String`，功能说明。
- `inputSchema`：`Object`，输入参数结构定义。
- `outputSchema`：`Object`，输出结果结构定义。
- `requiresApproval`：`Boolean`，是否需要审批。
- `sensitivity`：`String`，敏感等级。
- `version`：`String`，版本号。

---

### 1. 接口名
调用 MCP 工具

### 2. 方法类型
`POST`

### 3. 方法路径
`/mcp/tools/call`

### 4. 详细职责
该接口用于前端直接发起一次工具调用，常见于工具调试台、管理员测试面板、手工调用某个能力的场景。后端会根据 `serverCode` 决定走本地还是远端 MCP 服务，再把 `toolName` 和 `argumentsJson` 透传给底层适配器。它适合“我要显式调用某个工具并查看原始结果”的场景，不适合普通聊天页把自然语言直接发给它。

### 5. 是否鉴权
是。
需要 `Authorization` 请求头中的有效 JWT。当前源码未发现额外角色权限控制。

### 6. 入参字段和类型
- 请求体为 JSON 对象。
- `serverCode`：`String`，非必填，目标服务编码；为空时底层会默认路由到本地服务 `local-agent-server`。
- `toolName`：`String`，建议必填，目标工具名称；控制器不做强校验，但底层调用需要该值才能命中目标工具。
- `argumentsJson`：`String`，非必填，工具参数 JSON 字符串；HTTP 控制器本身不补默认值，底层远端适配器在部分链路下会把空值补成 `{}`。

### 7. 回参字段和类型
- HTTP `200` 成功时返回 `McpToolCallResult`。
- `status`：`String`，调用状态，例如成功、失败、等待中；具体取值以目标 MCP 服务返回为准。
- `serverCode`：`String`，实际执行服务编码。
- `toolName`：`String`，实际执行工具名。
- `data`：`Object`，结构化结果对象。
- `rawResult`：`String`，原始文本结果。

---

### 1. 接口名
查询 MCP 提示词目录

### 2. 方法类型
`GET`

### 3. 方法路径
`/mcp/prompts/list`

### 4. 详细职责
该接口用于前端查看某个服务下有哪些 MCP Prompt 可用，适合用在 Prompt 市场、Prompt 调试台、参数表单生成页。它只返回 Prompt 描述信息，不返回 Prompt 渲染结果；如果前端需要实际拿到模板展开内容，应继续调用 `/mcp/prompts/get`。

### 5. 是否鉴权
是。
需要 `Authorization` 请求头中的有效 JWT。当前源码未发现额外角色权限控制。

### 6. 入参字段和类型
- 查询参数 `serverCode`：`String`，非必填，目标服务编码；为空时底层会默认路由到本地服务 `local-agent-server`。

### 7. 回参字段和类型
- HTTP `200` 成功时返回 `Array<McpPromptDescriptor>`。
- `serverCode`：`String`，服务编码。
- `promptName`：`String`，Prompt 名称。
- `title`：`String`，展示标题。
- `description`：`String`，用途说明。
- `argumentsSchema`：`Object`，入参结构定义。
- `version`：`String`，版本号。

---

### 1. 接口名
读取 MCP 提示词

### 2. 方法类型
`POST`

### 3. 方法路径
`/mcp/prompts/get`

### 4. 详细职责
该接口用于前端按 Prompt 名称读取某个具体 Prompt，并可附带参数让后端进行模板展开。常见场景是 Prompt 调试、模板预览、Prompt 执行面板或需要把 Prompt 内容呈现给管理员查看的时候。它和 Prompt 治理后台接口不同，这里是 MCP 协议侧的 Prompt 调用入口，不是治理库里的 Prompt 条目管理入口。

### 5. 是否鉴权
是。
需要 `Authorization` 请求头中的有效 JWT。当前源码未发现额外角色权限控制。

### 6. 入参字段和类型
- 请求体为 JSON 对象。
- `serverCode`：`String`，非必填，目标服务编码；为空时底层会默认路由到本地服务 `local-agent-server`。
- `promptName`：`String`，建议必填，目标 Prompt 名称。
- `argumentsJson`：`String`，非必填，Prompt 参数 JSON 字符串；底层远端适配器在部分链路下会为空值补成 `{}`。

### 7. 回参字段和类型
- HTTP `200` 成功时返回 `McpPromptResult`。
- `status`：`String`，调用状态。
- `serverCode`：`String`，实际执行服务编码。
- `promptName`：`String`，Prompt 名称。
- `promptContent`：`Object`，Prompt 渲染结果对象；子字段由目标服务定义，源码未统一约束。

---

### 1. 接口名
查询 MCP 资源目录

### 2. 方法类型
`GET`

### 3. 方法路径
`/mcp/resources/list`

### 4. 详细职责
该接口用于前端查看某个 MCP 服务下可读取的资源列表，例如文档资源、配置资源、上下文资源等。适合用在资源浏览页、资源选择器、调试台。它返回的是资源“目录项”，而不是资源内容本身；如果前端已经选定某个资源 URI，要拿内容则继续调用 `/mcp/resources/read`。

### 5. 是否鉴权
是。
需要 `Authorization` 请求头中的有效 JWT。当前源码未发现额外角色权限控制。

### 6. 入参字段和类型
- 查询参数 `serverCode`：`String`，非必填，目标服务编码；为空时底层会默认路由到本地服务 `local-agent-server`。

### 7. 回参字段和类型
- HTTP `200` 成功时返回 `Array<McpResourceDescriptor>`。
- `serverCode`：`String`，服务编码。
- `resourceUri`：`String`，资源 URI。
- `name`：`String`，资源展示名。
- `description`：`String`，资源用途说明。
- `mimeType`：`String`，MIME 类型。
- `annotations`：`Object`，扩展注解信息。

---

### 1. 接口名
读取 MCP 资源内容

### 2. 方法类型
`POST`

### 3. 方法路径
`/mcp/resources/read`

### 4. 详细职责
该接口用于前端根据资源 URI 读取资源的实际内容。典型用法是前端先通过资源目录接口选中某个资源，再调用本接口拿内容用于展示、下载或调试。它返回的是“资源读取结果”而不是统一的文件流，因此前端应该把 `data` 当成结构化内容解析，而不是假定它一定是文本或二进制流。

### 5. 是否鉴权
是。
需要 `Authorization` 请求头中的有效 JWT。当前源码未发现额外角色权限控制。

### 6. 入参字段和类型
- 请求体为 JSON 对象。
- `serverCode`：`String`，非必填，目标服务编码；为空时底层会默认路由到本地服务 `local-agent-server`。
- `resourceUri`：`String`，建议必填，资源 URI。

### 7. 回参字段和类型
- HTTP `200` 成功时返回 `McpResourceResult`。
- `status`：`String`，读取状态。
- `serverCode`：`String`，实际执行服务编码。
- `resourceUri`：`String`，资源 URI。
- `mimeType`：`String`，MIME 类型。
- `data`：`Object`，资源实际内容对象；具体子字段由目标资源决定，源码未统一约束。

---

### 1. 接口名
同步能力目录

### 2. 方法类型
`POST`

### 3. 方法路径
`/mcp/catalog/sync`

### 4. 详细职责
该接口用于让前端主动触发一次 MCP 能力目录同步。适合出现在管理员能力中心的“刷新目录”按钮或调试后台中。调用后后端会启动能力目录同步流程并立即返回触发成功提示，因此前端应把它当成“触发动作”而不是“同步完成结果”，不要把返回值误解为最终同步明细。

### 5. 是否鉴权
是。
需要 `Authorization` 请求头中的有效 JWT。当前源码未发现额外角色权限控制。

### 6. 入参字段和类型
- 请求体：无。
- 查询参数：无。

### 7. 回参字段和类型
- HTTP `200` 成功时返回 JSON 对象。
- `status`：`String`，值为 `success`。
- `message`：`String`，固定为 `capability catalog sync triggered`。

---

### 1. 接口名
写入 MCP 服务注册表

### 2. 方法类型
`POST`

### 3. 方法路径
`/mcp/migrate/server-registry`

### 4. 详细职责
该接口用于前端或后台管理端写入、更新一条 MCP 服务注册信息。通常用于新增一个远端 MCP 服务、修改基础地址、调整鉴权配置或启停服务。后端会按 `serverCode` 判定是新增还是覆盖更新，因此前端在编辑模式下可直接提交完整对象。

### 5. 是否鉴权
是。
需要 `Authorization` 请求头中的有效 JWT。当前源码未发现额外角色权限控制。

### 6. 入参字段和类型
- 请求体为 `McpServerRegistry` 对象。
- `id`：`Long`，非必填，主键。
- `serverCode`：`String`，必填，服务编码。
- `serverName`：`String`，非必填，服务名。
- `description`：`String`，非必填，服务描述。
- `baseUrl`：`String`，非必填，服务基础地址。
- `transportType`：`String`，非必填，传输方式，例如 HTTP、SSE、RPC、WS、STDIO、LOCAL；源码未统一枚举校验。
- `authType`：`String`，非必填，鉴权类型，例如 `BEARER`、`BASIC`。
- `authConfig`：`Object`，非必填，鉴权配置 JSON。
- `enabled`：`Boolean`，非必填，是否启用。
- `healthStatus`：`String`，非必填，健康状态。
- `lastSyncAt`：`String`，非必填，最后同步时间，序列化格式取决于 `LocalDateTime`。
- `createdAt`：`String`，非必填，创建时间。
- `updatedAt`：`String`，非必填，更新时间。

### 7. 回参字段和类型
- HTTP `200` 成功时返回 `McpServerRegistry` 对象，字段与入参对象一致。
- 如果 `serverCode` 缺失，源码会抛出 `IllegalArgumentException`，通常会进入全局异常处理并返回 HTTP `500`。

---

### 1. 接口名
写入 MCP 工具目录

### 2. 方法类型
`POST`

### 3. 方法路径
`/mcp/migrate/tool-catalog`

### 4. 详细职责
该接口用于把工具目录写入本地目录表，适合管理员或迁移后台同步远端工具元数据。后端会按 `serverCode + toolName` 去重，存在则更新，不存在则新增，并自动补充执行模式和语义向量。前端通常只会在管理后台或初始化导入工具时调用。

### 5. 是否鉴权
是。
需要 `Authorization` 请求头中的有效 JWT。当前源码未发现额外角色权限控制。

### 6. 入参字段和类型
- 请求体为 `McpToolCatalog` 对象。
- `id`：`Long`，非必填，主键。
- `serverCode`：`String`，必填，服务编码。
- `toolName`：`String`，必填，工具名。
- `title`：`String`，非必填，标题。
- `description`：`String`，非必填，描述。
- `inputSchema`：`Object`，非必填，输入 Schema。
- `outputSchema`：`Object`，非必填，输出 Schema。
- `annotations`：`Object`，非必填，扩展注解。
- `tags`：`Array<String>`，非必填，标签列表。
- `enabled`：`Boolean`，非必填，是否启用。
- `version`：`String`，非必填，版本。
- `executionMode`：`String`，非必填，执行模式；源码会归一化为 `MCP` 或 `LEGACY`，非法值回退 `MCP`。
- `requiresApproval`：`Boolean`，非必填，是否需要审批。
- `sensitivity`：`String`，非必填，敏感等级。
- `rawPayload`：`Object`，非必填，原始目录载荷。
- `embedding`：`String`，非必填，语义向量；后端可能自动重算。
- `syncedAt`：`String`，非必填，最近同步时间。
- `createdAt`：`String`，非必填，创建时间。
- `updatedAt`：`String`，非必填，更新时间。

### 7. 回参字段和类型
- HTTP `200` 成功时返回 `McpToolCatalog` 对象，字段与入参对象一致。
- 如果缺少 `serverCode` 或 `toolName`，通常会进入全局异常处理并返回 HTTP `500`。

---

### 1. 接口名
写入 MCP 工具实现映射

### 2. 方法类型
`POST`

### 3. 方法路径
`/mcp/migrate/tool-impl-mapping`

### 4. 详细职责
该接口用于配置某个工具在本地系统中应该怎样真正执行，例如走本地处理器、HTTP、RPC、工作流或 Spring Bean。它通常只在管理员配置“工具目录与实现绑定关系”时使用，不属于终端用户功能页面。前端调用时要特别注意实现类型和启用条件，因为后端会对高风险实现方式做额外限制。

### 5. 是否鉴权
是。
需要 `Authorization` 请求头中的有效 JWT。当前源码未发现额外角色权限控制。

### 6. 入参字段和类型
- 请求体为 `McpToolImplMapping` 对象。
- `id`：`Long`，非必填，主键。
- `serverCode`：`String`，必填，服务编码。
- `toolName`：`String`，必填，工具名。
- `implType`：`String`，非必填，实现类型；源码允许 `LOCAL_HANDLER`、`HTTP`、`RPC`、`WORKFLOW`、`SPRING_BEAN`，为空默认 `LOCAL_HANDLER`。
- `executionMode`：`String`，非必填，执行模式；源码会归一化为 `MCP` 或 `LEGACY`。
- `beanName`：`String`，非必填，Spring Bean 名。
- `methodName`：`String`，非必填，Bean 方法名。
- `routeUri`：`String`，非必填，远端路由地址。
- `timeoutMs`：`Integer`，非必填，超时毫秒。
- `retryPolicy`：`Object`，非必填，重试策略配置。
- `enabled`：`Boolean`，非必填，是否启用。
- `createdAt`：`String`，非必填，创建时间。
- `updatedAt`：`String`，非必填，更新时间。

### 7. 回参字段和类型
- HTTP `200` 成功时返回 `McpToolImplMapping` 对象，字段与入参对象一致。
- 如果缺少 `serverCode` 或 `toolName`，或者 `implType` 不支持，通常会进入全局异常处理并返回 HTTP `500`。
- 当 `implType=SPRING_BEAN` 且 `enabled=true`，但配置 `luna.mcp.execution.allow-spring-bean=false` 时，也会报错。

---

### 1. 接口名
写入 MCP 提示词目录

### 2. 方法类型
`POST`

### 3. 方法路径
`/mcp/migrate/prompt-catalog`

### 4. 详细职责
该接口用于新增或更新 MCP Prompt 目录元数据，常见于管理后台同步远端 Prompt 列表、人工维护 Prompt 目录、导入初始化数据。后端按 `serverCode + promptName` 判断新增还是更新，并会自动尝试补充语义向量，前端可以把它当成一条“目录 upsert”接口来使用。

### 5. 是否鉴权
是。
需要 `Authorization` 请求头中的有效 JWT。当前源码未发现额外角色权限控制。

### 6. 入参字段和类型
- 请求体为 `McpPromptCatalog` 对象。
- `id`：`Long`，非必填，主键。
- `serverCode`：`String`，必填，服务编码。
- `promptName`：`String`，必填，Prompt 名称。
- `title`：`String`，非必填，标题。
- `description`：`String`，非必填，描述。
- `argumentsSchema`：`Object`，非必填，参数 Schema。
- `rawPayload`：`Object`，非必填，原始目录载荷。
- `tags`：`Array<String>`，非必填，标签。
- `enabled`：`Boolean`，非必填，是否启用。
- `version`：`String`，非必填，版本号。
- `embedding`：`String`，非必填，语义向量；后端可能自动重算。
- `syncedAt`：`String`，非必填，同步时间。
- `createdAt`：`String`，非必填，创建时间。
- `updatedAt`：`String`，非必填，更新时间。

### 7. 回参字段和类型
- HTTP `200` 成功时返回 `McpPromptCatalog` 对象，字段与入参对象一致。
- 缺少 `serverCode` 或 `promptName` 时，通常会进入全局异常处理并返回 HTTP `500`。

---

### 1. 接口名
写入 MCP 资源目录

### 2. 方法类型
`POST`

### 3. 方法路径
`/mcp/migrate/resource-catalog`

### 4. 详细职责
该接口用于新增或更新 MCP 资源目录项。适合资源中心、管理后台、同步程序调用，用来维护 `resourceUri` 级别的资源元数据。前端调用后端会按 `serverCode + resourceUri` 去重写入，并自动尝试补充语义向量。

### 5. 是否鉴权
是。
需要 `Authorization` 请求头中的有效 JWT。当前源码未发现额外角色权限控制。

### 6. 入参字段和类型
- 请求体为 `McpResourceCatalog` 对象。
- `id`：`Long`，非必填，主键。
- `serverCode`：`String`，必填，服务编码。
- `resourceUri`：`String`，必填，资源 URI。
- `name`：`String`，非必填，名称。
- `description`：`String`，非必填，描述。
- `mimeType`：`String`，非必填，MIME 类型。
- `annotations`：`Object`，非必填，扩展注解。
- `rawPayload`：`Object`，非必填，原始目录载荷。
- `tags`：`Array<String>`，非必填，标签。
- `enabled`：`Boolean`，非必填，是否启用。
- `embedding`：`String`，非必填，语义向量；后端可能自动重算。
- `syncedAt`：`String`，非必填，同步时间。
- `createdAt`：`String`，非必填，创建时间。
- `updatedAt`：`String`，非必填，更新时间。

### 7. 回参字段和类型
- HTTP `200` 成功时返回 `McpResourceCatalog` 对象，字段与入参对象一致。
- 缺少 `serverCode` 或 `resourceUri` 时，通常会进入全局异常处理并返回 HTTP `500`。

---

### 1. 接口名
写入工作流模板

### 2. 方法类型
`POST`

### 3. 方法路径
`/mcp/migrate/workflow-template`

### 4. 详细职责
该接口用于新增或更新本地工作流模板。适合工作流后台、模板配置台或迁移工具调用。它不是“执行工作流”的接口，而是“写模板目录”的接口，前端在这里提交的是模板元数据、输入输出 Schema、依赖能力、蓝图 JSON 等描述信息。

### 5. 是否鉴权
是。
需要 `Authorization` 请求头中的有效 JWT。当前源码未发现额外角色权限控制。

### 6. 入参字段和类型
- 请求体为 `WorkflowTemplate` 对象。
- `id`：`Long`，非必填，主键。
- `workflowName`：`String`，必填，工作流名称。
- `description`：`String`，非必填，描述。
- `inputSchema`：`Object`，非必填，输入 Schema。
- `outputSchema`：`Object`，非必填，输出 Schema。
- `requiredCapabilities`：`Array<String>`，非必填，依赖能力列表。
- `toolSlots`：`Array<Object>`，非必填，工具插槽列表。
- `toolSlots[].slot`：`String`，插槽名。
- `toolSlots[].capability`：`String`，能力要求。
- `toolSlots[].required`：`Boolean`，是否必填。
- `thoughtChain`：`Array<String>`，非必填，思维链或执行提示列表。
- `blueprintJson`：`Object`，非必填，工作流蓝图 JSON。
- `enabled`：`Boolean`，非必填，是否启用。
- `version`：`String`，非必填，版本号。
- `embedding`：`String`，非必填，语义向量；后端可能自动重算。
- `createdAt`：`String`，非必填，创建时间。
- `updatedAt`：`String`，非必填，更新时间。

### 7. 回参字段和类型
- HTTP `200` 成功时返回 `WorkflowTemplate` 对象，字段与入参对象一致。
- 缺少 `workflowName` 时，通常会进入全局异常处理并返回 HTTP `500`。

---

### 1. 接口名
MCP JSON-RPC 统一调用入口

### 2. 方法类型
`POST`

### 3. 方法路径
`/mcp/rpc`

### 4. 详细职责
该接口用于让前端或 Agent 侧通过 JSON-RPC 协议统一调用 MCP 工具、Prompt 和资源能力。当前项目已经把普通 REST 风格的 `tools/list`、`tools/call`、`prompts/list`、`prompts/get`、`resources/list`、`resources/read` 六类能力路由进了这个统一入口，因此如果前端有一个标准 JSON-RPC 客户端，直接接这个入口会比逐个 REST 接口更统一。它特别适合协议调试页、通用 Agent 调用器、或者一个前端面板同时兼容多个 MCP 方法的场景。

### 5. 是否鉴权
是。
需要 `Authorization` 请求头中的有效 JWT。当前源码未发现额外角色权限控制。

### 6. 入参字段和类型
- 请求体为 JSON-RPC 对象。
- `jsonrpc`：`String`，非必填，源码不会校验该字段，但建议传 `2.0`。
- `id`：`String` 或 `Number` 或 `Object`，非必填，请求 ID；后端会原样回显。
- `method`：`String`，必填，方法名。
- `method` 支持的值有：
- `tools/list`
- `tools/call`
- `prompts/list`
- `prompts/get`
- `resources/list`
- `resources/read`
- `mcp.tools.list`
- `mcp.tools.call`
- `mcp.prompts.list`
- `mcp.prompts.get`
- `mcp.resources.list`
- `mcp.resources.read`
- `params`：`Object`，非必填，请求参数对象。
- `params.serverCode`：`String`，方法相关，非必填；为空时底层通常默认走本地服务 `local-agent-server`。
- `params.toolName`：`String`，调用工具时建议必填。
- `params.promptName`：`String`，读取 Prompt 时建议必填。
- `params.resourceUri`：`String`，读取资源时建议必填。
- `params.argumentsJson`：`String`，工具调用或 Prompt 获取时非必填；如果为空，控制器会默认补为 `{}`。

### 7. 回参字段和类型
- 成功时返回标准 JSON-RPC 对象。
- `jsonrpc`：`String`，固定为 `2.0`。
- `id`：与请求 `id` 相同，可能为任意 JSON 类型。
- `result`：`Object` 或 `Array`，具体结构取决于所调用的方法。
- `tools/list` 或 `mcp.tools.list` 的 `result` 为 `Array<McpToolDescriptor>`。
- `tools/call` 或 `mcp.tools.call` 的 `result` 为 `McpToolCallResult`。
- `prompts/list` 或 `mcp.prompts.list` 的 `result` 为 `Array<McpPromptDescriptor>`。
- `prompts/get` 或 `mcp.prompts.get` 的 `result` 为 `McpPromptResult`。
- `resources/list` 或 `mcp.resources.list` 的 `result` 为 `Array<McpResourceDescriptor>`。
- `resources/read` 或 `mcp.resources.read` 的 `result` 为 `McpResourceResult`。
- 失败时返回标准 JSON-RPC 错误对象。
- `jsonrpc`：`String`，固定为 `2.0`。
- `id`：与请求 `id` 相同。
- `error`：`Object`，错误对象。
- `error.code`：`Number`，源码明确可能为 `-32600`、`-32601`、`-32603`。
- `error.message`：`String`，错误信息。
- `-32600` 表示非法请求，常见于 `method` 缺失。
- `-32601` 表示方法不存在。
- `-32603` 表示服务内部异常。

## 查询接口

---

### 1. 接口名
分页查询知识库

### 2. 方法类型
`POST`

### 3. 方法路径
`/luna/api/query/knowledge-base`
`/luna/api/query/knowledge-base/page`

### 4. 详细职责
该接口用于后台或前端管理页按条件分页查看知识库片段。适合知识库管理、知识检索调试、内容运营查看导入结果时调用。它支持分页、标题和内容模糊搜索、来源类型过滤、来源路径过滤、时间范围过滤；返回结果已经封装成统一分页结构，前端可以直接绑定分页器和列表组件。

### 5. 是否鉴权
是。
需要 `Authorization` 请求头中的有效 JWT。当前源码未发现额外角色权限控制。

### 6. 入参字段和类型
- 请求体为 JSON 对象，可为空；为空时后端会使用默认分页参数。
- `pageNo`：`Long`，非必填，页码，从 `1` 开始，默认值 `1`。
- `pageSize`：`Long`，非必填，每页条数，默认值 `10`，最大值 `200`。
- `title`：`String`，非必填，标题模糊查询。
- `content`：`String`，非必填，内容模糊查询。
- `sourceType`：`String`，非必填，来源类型；支持 `FILE`、`WEB_SEARCH`、`MANUAL_INPUT`。
- `sourcePath`：`String`，非必填，来源路径模糊查询。
- `startTime`：`String`，非必填，开始时间，格式必须为 `yyyy-MM-dd HH:mm:ss`。
- `endTime`：`String`，非必填，结束时间，格式必须为 `yyyy-MM-dd HH:mm:ss`。
- 排序：源码固定按 `created_at desc` 返回，前端不能自定义排序字段。

### 7. 回参字段和类型
- HTTP `200` 成功时返回 `PagedResponse<KnowledgeChunkRecord>`。
- `total`：`Long`，总记录数。
- `pages`：`Long`，总页数。
- `pageNo`：`Long`，当前页码。
- `pageSize`：`Long`，当前页大小。
- `records`：`Array<Object>`，当前页记录列表。
- `records[].id`：`Long`，联合结果主键。
- `records[].docId`：`Long`，文档 ID。
- `records[].chunkId`：`Long`，分片 ID。
- `records[].chunkOrder`：`Integer`，分片顺序。
- `records[].title`：`String`，标题。
- `records[].content`：`String`，正文内容。
- `records[].sourceType`：`String`，来源类型。
- `records[].sourcePath`：`String`，来源路径。
- `records[].embedding`：`String`，向量数据文本。
- `records[].vectorScore`：`Double`，向量检索得分；分页查询场景通常可能为空。
- `records[].ftsScore`：`Double`，全文检索得分；分页查询场景通常可能为空。
- `records[].createdAt`：`String`，创建时间。
- `records[].updatedAt`：`String`，更新时间。
- HTTP `500` 查询失败时返回 JSON 对象。
- `status`：`String`，值为 `error`。
- `message`：`String`，错误说明。

---

### 1. 接口名
分页查询运行日志

### 2. 方法类型
`POST`

### 3. 方法路径
`/luna/api/query/log`
`/luna/api/query/log/page`

### 4. 详细职责
该接口用于前端后台按条件分页查看系统日志。适合日志中心、问题排查页、链路查询页、工具调用审计页调用。它支持日志类型、模块、动作、内容、traceId、operatorId、时间范围等筛选条件，后端按非空条件动态拼接查询，并固定按创建时间倒序返回，前端可以直接做筛选表单和分页列表。

### 5. 是否鉴权
是。
需要 `Authorization` 请求头中的有效 JWT。当前源码未发现额外角色权限控制。

### 6. 入参字段和类型
- 请求体为 JSON 对象，可为空；为空时后端会使用默认分页参数。
- `pageNo`：`Long`，非必填，页码，从 `1` 开始，默认值 `1`。
- `pageSize`：`Long`，非必填，每页条数，默认值 `10`，最大值 `200`。
- `logType`：`String`，非必填，日志类型；支持 `LUNA_OUTPUT`、`TOOL_CALL`、`ERROR`、`SELF_UPDATE`、`SYSTEM_EVENT`、`API_CALL`。
- `module`：`String`，非必填，模块模糊查询。
- `action`：`String`，非必填，动作模糊查询。
- `content`：`String`，非必填，内容模糊查询。
- `traceId`：`String`，非必填，链路追踪 ID 精确查询。
- `operatorId`：`String`，非必填，操作人 ID 精确查询。
- `startTime`：`String`，非必填，开始时间，格式必须为 `yyyy-MM-dd HH:mm:ss`。
- `endTime`：`String`，非必填，结束时间，格式必须为 `yyyy-MM-dd HH:mm:ss`。
- 排序：源码固定按 `createAt desc` 返回，前端不能自定义排序字段。

### 7. 回参字段和类型
- HTTP `200` 成功时返回 `PagedResponse<LunaLog>`。
- `total`：`Long`，总记录数。
- `pages`：`Long`，总页数。
- `pageNo`：`Long`，当前页码。
- `pageSize`：`Long`，当前页大小。
- `records`：`Array<Object>`，当前页日志列表。
- `records[].id`：`Long`，日志 ID。
- `records[].logType`：`String`，日志类型。
- `records[].module`：`String`，模块名。
- `records[].action`：`String`，动作名。
- `records[].content`：`String`，日志正文。
- `records[].requestData`：`Object`，请求侧附加数据。
- `records[].responseData`：`Object`，响应侧附加数据。
- `records[].errorMessage`：`String`，异常简要信息。
- `records[].errorStack`：`String`，异常堆栈。
- `records[].costTime`：`Long`，耗时毫秒。
- `records[].operatorId`：`String`，操作人 ID。
- `records[].traceId`：`String`，链路追踪 ID。
- `records[].createAt`：`String`，创建时间。
- HTTP `400` 参数错误时返回 JSON 对象。
- `status`：`String`，值为 `error`。
- `message`：`String`，具体参数错误说明。
- HTTP `500` 查询失败时返回 JSON 对象。
- `status`：`String`，值为 `error`。
- `message`：`String`，查询失败说明。

---

### 1. 接口名
旧版内存分页查询接口（已退役）

### 2. 方法类型
`POST`

### 3. 方法路径
`/luna/api/query/memory`
`/luna/api/query/memory/page`

### 4. 详细职责
该接口是历史内存查询路径，占位保留仅用于兼容提示，不再真正查询任何数据。前端如果还存在旧版“内存列表”“长期记忆管理”等页面，调用这个接口不会得到记录列表，而只会得到明确的退役提示。新对接应改查新的运行态内存表或新的 v2 内存体系。

### 5. 是否鉴权
是。
需要 `Authorization` 请求头中的有效 JWT。当前源码未发现额外角色权限控制。

### 6. 入参字段和类型
- 请求体为 JSON 对象，可为空，但源码实际不会读取这些筛选条件。
- `pageNo`：`Long`，非必填，页码，默认值 `1`。
- `pageSize`：`Long`，非必填，分页大小，默认值 `10`，最大 `200`。
- `sessionId`：`String`，非必填，会话 ID。
- `memoryType`：`String`，非必填，记忆类型，示例 `FACT`、`PREFERENCE`、`SUMMARY`、`REFLECTION`。
- `content`：`String`，非必填，内容模糊查询。
- `minWeight`：`Integer`，非必填，最小权重。
- `maxWeight`：`Integer`，非必填，最大权重。
- `startTime`：`String`，非必填，开始时间，格式 `yyyy-MM-dd HH:mm:ss`。
- `endTime`：`String`，非必填，结束时间，格式 `yyyy-MM-dd HH:mm:ss`。

### 7. 回参字段和类型
- 统一返回 HTTP `410`。
- `status`：`String`，值为 `error`。
- `message`：`String`，固定为 `legacy luna_memory API retired, use v2 runtime memory tables`。

---

### 1. 接口名
旧版用户偏好分页查询接口（已退役）

### 2. 方法类型
`POST`

### 3. 方法路径
`/luna/api/query/user-preference`
`/luna/api/query/user-preference/page`

### 4. 详细职责
该接口是旧版用户偏好查询入口，当前仅保留作迁移提示，不再执行真实查询。前端如果还存在历史“偏好管理”“旧偏好列表”等页面，调用时只会收到退役提示，不会返回分页数据。新对接应切换到新的关系语义事实表相关能力。

### 5. 是否鉴权
是。
需要 `Authorization` 请求头中的有效 JWT。当前源码未发现额外角色权限控制。

### 6. 入参字段和类型
- 请求体为 JSON 对象，可为空，但源码实际不会读取这些筛选条件。
- `pageNo`：`Long`，非必填，页码，默认值 `1`。
- `pageSize`：`Long`，非必填，分页大小，默认值 `10`，最大 `200`。
- `prefKey`：`String`，非必填，偏好键模糊查询。
- `prefValue`：`String`，非必填，偏好值模糊查询。
- `description`：`String`，非必填，描述模糊查询。
- `startTime`：`String`，非必填，开始时间，格式 `yyyy-MM-dd HH:mm:ss`。
- `endTime`：`String`，非必填，结束时间，格式 `yyyy-MM-dd HH:mm:ss`。

### 7. 回参字段和类型
- 统一返回 HTTP `410`。
- `status`：`String`，值为 `error`。
- `message`：`String`，固定为 `legacy user_preference API retired, use relational_semantic_fact`。

## Prompt 治理接口

---

### 1. 接口名
查询 Prompt 分类列表

### 2. 方法类型
`GET`

### 3. 方法路径
`/api/prompt/categories`

### 4. 详细职责
该接口用于 Prompt 治理后台初始化分类筛选项。前端进入 Prompt 管理列表页、筛选面板、创建表单时，可以先调用这个接口拿到当前系统可用的一级分类键列表，再驱动分类下拉框、检索条件和默认选项。它返回的是“可用分类键”的轻量结果，不包含分类描述和树结构，适合做快速初始化；如果页面还需要展示分类名称、排序、是否执行类等信息，应继续调用分类详情接口或分类树接口。

### 5. 是否鉴权
是。
需要 `Authorization` 请求头中的有效 JWT。当前源码未发现额外角色权限控制。

### 6. 入参字段和类型
- 无请求体。
- 无查询参数。

### 7. 回参字段和类型
- 成功时返回 `Array<String>`。
- 数组每一项：`String`，分类键，例如某个一级分类的 `categoryKey`。
- 返回内容优先来自 `prompt_category` 表中的启用分类；若分类表无结果，源码会退回到内置 Prompt 或当前已启用 Prompt 条目反推分类。
- 失败时通常返回文档开头“统一异常与错误返回”中的错误结构。

---

### 1. 接口名
查询 Prompt 分类详情列表

### 2. 方法类型
`GET`

### 3. 方法路径
`/api/prompt/categories/detail`

### 4. 详细职责
该接口用于 Prompt 治理后台展示“分类列表”而不是简单下拉框。前端在分类管理页、创建 Prompt 时需要展示分类中文名、排序、是否允许关键词匹配、是否执行类分类时，应调用这个接口。它返回的是已经适合前端直接渲染的扁平详情结构，适合表单联动、分类说明展示和分类维度筛选。

### 5. 是否鉴权
是。
需要 `Authorization` 请求头中的有效 JWT。当前源码未发现额外角色权限控制。

### 6. 入参字段和类型
- 无请求体。
- 无查询参数。

### 7. 回参字段和类型
- 成功时返回 `Array<Object>`。
- `[].categoryKey`：`String`，分类唯一键。
- `[].categoryName`：`String`，分类展示名称。
- `[].sortOrder`：`Integer`，排序值，数值越小越靠前。
- `[].keywordMatchAllowed`：`Boolean`，该分类下是否允许关键词匹配。
- `[].executionCategory`：`Boolean`，是否为执行类分类。
- `[].enabled`：`Boolean`，是否启用。
- 当前接口只返回已启用分类。
- 失败时通常返回文档开头“统一异常与错误返回”中的错误结构。

---

### 1. 接口名
查询 Prompt 分类树

### 2. 方法类型
`GET`

### 3. 方法路径
`/api/prompt/categories/tree`

### 4. 详细职责
该接口用于需要树形分类导航的前端页面，例如 Prompt 分类树筛选器、左侧分类导航、按父子层级浏览的配置页面。当前端不只需要平铺列表，而是需要知道分类层级关系、父子节点结构、递归 children 数据时，应调用这个接口。它会直接返回已经组装好的树结构，前端可以直接绑定 Tree 组件或级联选择器。

### 5. 是否鉴权
是。
需要 `Authorization` 请求头中的有效 JWT。当前源码未发现额外角色权限控制。

### 6. 入参字段和类型
- 无请求体。
- 无查询参数。

### 7. 回参字段和类型
- 成功时返回 `Array<Object>`，每一项都是树节点。
- `[].categoryKey`：`String`，分类唯一键。
- `[].categoryName`：`String`，分类名称。
- `[].parentCategoryKey`：`String`，父级分类键；根节点通常为空字符串。
- `[].sortOrder`：`Integer`，排序值。
- `[].keywordMatchAllowed`：`Boolean`，该分类下是否允许关键词匹配。
- `[].executionCategory`：`Boolean`，是否执行类分类。
- `[].enabled`：`Boolean`，是否启用。
- `[].children`：`Array<Object>`，子节点数组，字段结构与父节点相同，按相同规则递归展开。
- 当前接口只返回已启用分类树。
- 失败时通常返回文档开头“统一异常与错误返回”中的错误结构。

---

### 1. 接口名
按分类查询 Prompt 键值视图

### 2. 方法类型
`GET`

### 3. 方法路径
`/api/prompt/items`

### 4. 详细职责
该接口用于按分类快速浏览 Prompt 内容，适合前端做“某分类下有哪些 Prompt”的轻量展示、快速选择器、只读预览面板。前端通常在用户先选一级分类，再可选二级分类后调用它，用于得到当前分类下的 Prompt 键值映射。该接口内部固定只查启用中的 Prompt，并且固定使用 `pageNo=1`、`pageSize=500` 的搜索策略，因此它适合轻量浏览，不适合做完整后台搜索分页。

### 5. 是否鉴权
是。
需要 `Authorization` 请求头中的有效 JWT。当前源码未发现额外角色权限控制。

### 6. 入参字段和类型
- `category`：`String`，必填，一级分类键。
- `subCategory`：`String`，非必填，二级分类键；不传时返回该一级分类下全部已启用 Prompt。
- 分页、排序说明：源码固定按内部搜索结果返回，不开放前端自定义分页和排序参数；内部固定只取前 `500` 条启用项。

### 7. 回参字段和类型
- 成功时返回 `Object`。
- `category`：`String`，当前请求使用的一级分类。
- `subCategory`：`String`，当前请求使用的二级分类；未传时返回空字符串。
- `items`：`Object`，键值映射结构。
- `items.{promptKey}`：`String`，Prompt 正文内容；对象的 key 就是 Prompt 唯一键。
- 当前接口只返回已启用 Prompt，不返回 disabled 项，也不返回版本信息。
- 失败时通常返回文档开头“统一异常与错误返回”中的错误结构。

---

### 1. 接口名
按 Key 查询 Prompt 详情

### 2. 方法类型
`GET`

### 3. 方法路径
`/api/prompt/item/detail`

### 4. 详细职责
该接口用于 Prompt 详情页、编辑页回填、从列表点击某个 Prompt 后查看完整配置时调用。前端拿到 Prompt 唯一键后，可以通过它一次性获取条目层信息、当前展示版本信息、匹配范围、编辑策略等完整字段。与分类浏览接口不同，这个接口适合做单条记录的精细查看。源码明确允许读取已禁用条目，因此后台页面即使在“回收站”“历史停用项”场景下也可以使用本接口。

### 5. 是否鉴权
是。
需要 `Authorization` 请求头中的有效 JWT。当前源码未发现额外角色权限控制。

### 6. 入参字段和类型
- `key`：`String`，必填，Prompt 唯一键。
- 源码会按 key 精确查询，并兼容别名 key；如果启用了 `prompt.governance.builtin-fallback-enabled`，还可能命中内置 Prompt。

### 7. 回参字段和类型
- 成功时返回 `PromptItemRecord`；未命中时返回空对象 `{}`。
- `itemId`：`Long`，Prompt 条目主记录 ID。
- `versionId`：`Long`，当前展示版本 ID。
- `key`：`String`，Prompt 唯一键。
- `name`：`String`，Prompt 名称。
- `value`：`String`，Prompt 正文内容。
- `category`：`String`，一级分类键。
- `subCategory`：`String`，二级分类键。
- `description`：`String`，说明描述。
- `runtimeSlot`：`String`，运行时槽位。
- `hasTemplateVariables`：`Boolean`，是否包含模板变量。
- `templateVariables`：`Array<String>`，模板变量列表。
- `keywordMatchEnabled`：`Boolean`，是否启用关键词匹配。
- `matchKeywords`：`Array<String>`，关键词列表。
- `assemblyMode`：`String`，组装模式。
- `matchScope`：`Object`，匹配范围配置。
- `matchScope.agents`：`Array<String>`，允许命中的代理标识列表。
- `matchScope.nodeKinds`：`Array<String>`，允许命中的节点类型列表。
- `matchScope.taskStates`：`Array<String>`，允许命中的任务状态列表。
- `matchScope.modelFamilies`：`Array<String>`，允许命中的模型家族列表。
- `matchScope.personaIds`：`Array<String>`，允许命中的人格标识列表。
- `matchScope.sceneIds`：`Array<String>`，允许命中的场景标识列表。
- `editPolicy`：`Object`，编辑策略配置。
- `editPolicy.create`：`Boolean`，是否允许创建同类 Prompt。
- `editPolicy.update`：`Boolean`，是否允许更新。
- `editPolicy.delete`：`Boolean`，是否允许删除。
- `enabled`：`Boolean`，条目是否启用。
- `priority`：`Integer`，优先级，值越大越优先。
- `status`：`String`，条目状态，常见为 `enabled` 或 `disabled`。
- `version`：`String`，版本号。
- `versionLabel`：`String`，版本展示标签。
- `changeNote`：`String`，版本变更说明。
- 失败时通常返回文档开头“统一异常与错误返回”中的错误结构。

---

### 1. 接口名
按 ID 查询 Prompt 详情

### 2. 方法类型
`GET`

### 3. 方法路径
`/api/prompt/item/detail-by-id`

### 4. 详细职责
该接口和按 key 查询详情的目标相同，但适合前端已经持有数据库主键 ID 的场景，例如版本列表跳详情、从别的管理页按主键联动查看。当前端并不方便传 key，而是直接拿到 `itemId` 时，可以调用此接口完成详情回填。源码同样允许读取已禁用条目。

### 5. 是否鉴权
是。
需要 `Authorization` 请求头中的有效 JWT。当前源码未发现额外角色权限控制。

### 6. 入参字段和类型
- `id`：`Long`，必填，Prompt 条目主键 ID。

### 7. 回参字段和类型
- 成功时返回 `PromptItemRecord`；未命中时返回空对象 `{}`。
- 返回字段与 `/api/prompt/item/detail` 完全一致：
- `itemId`：`Long`。
- `versionId`：`Long`。
- `key`：`String`。
- `name`：`String`。
- `value`：`String`。
- `category`：`String`。
- `subCategory`：`String`。
- `description`：`String`。
- `runtimeSlot`：`String`。
- `hasTemplateVariables`：`Boolean`。
- `templateVariables`：`Array<String>`。
- `keywordMatchEnabled`：`Boolean`。
- `matchKeywords`：`Array<String>`。
- `assemblyMode`：`String`。
- `matchScope`：`Object`。
- `matchScope.agents`：`Array<String>`。
- `matchScope.nodeKinds`：`Array<String>`。
- `matchScope.taskStates`：`Array<String>`。
- `matchScope.modelFamilies`：`Array<String>`。
- `matchScope.personaIds`：`Array<String>`。
- `matchScope.sceneIds`：`Array<String>`。
- `editPolicy`：`Object`。
- `editPolicy.create`：`Boolean`。
- `editPolicy.update`：`Boolean`。
- `editPolicy.delete`：`Boolean`。
- `enabled`：`Boolean`。
- `priority`：`Integer`。
- `status`：`String`。
- `version`：`String`。
- `versionLabel`：`String`。
- `changeNote`：`String`。
- 失败时通常返回文档开头“统一异常与错误返回”中的错误结构。

---

### 1. 接口名
校验 Prompt Key 是否存在

### 2. 方法类型
`GET`

### 3. 方法路径
`/api/prompt/item/exists`

### 4. 详细职责
该接口用于创建 Prompt 前做唯一性校验，也适合前端在用户输入 key 时进行实时校验，提前提示“已存在/可创建”。它不返回完整 Prompt 数据，只回答某个 key 是否能在当前注册视图里被识别为已存在。源码会同时考虑主 key、别名 key 和可选的内置 Prompt 兜底，因此这个接口比单纯查数据库更接近实际运行态可见结果。

### 5. 是否鉴权
是。
需要 `Authorization` 请求头中的有效 JWT。当前源码未发现额外角色权限控制。

### 6. 入参字段和类型
- `key`：`String`，必填，待校验的 Prompt 唯一键。

### 7. 回参字段和类型
- 成功时返回 `Object`。
- `key`：`String`，本次校验的 key。
- `exists`：`Boolean`，是否存在。
- 说明：源码的存在性判断会覆盖 key 别名；如果开启了 `prompt.governance.builtin-fallback-enabled`，内置 Prompt 也会被视为存在。
- 失败时通常返回文档开头“统一异常与错误返回”中的错误结构。

---

### 1. 接口名
查询分类下 Prompt 键值映射

### 2. 方法类型
`GET`

### 3. 方法路径
`/api/prompt/item/key-values`

### 4. 详细职责
该接口用于分类下拉联动、快速模板选择、只展示 key 和内容的轻量场景。与 `/api/prompt/items` 相比，它只接收一级分类，不包含 `subCategory` 参数，也不返回包装层中的 `category` 和 `subCategory`。当前端只需要某个分类下“有哪些有效 Prompt”及其文本内容时，用这个接口更直接。

### 5. 是否鉴权
是。
需要 `Authorization` 请求头中的有效 JWT。当前源码未发现额外角色权限控制。

### 6. 入参字段和类型
- `category`：`String`，必填，一级分类键。

### 7. 回参字段和类型
- 成功时返回 `Object`，实际是 `Map<String,String>`。
- `{promptKey}`：`String`，对象的 key 为 Prompt 唯一键。
- `{promptKey}` 对应的 value：`String`，Prompt 正文内容。
- 当前接口只返回 active Prompt，不返回 disabled 项，也不包含版本与策略字段。
- 失败时通常返回文档开头“统一异常与错误返回”中的错误结构。

---

### 1. 接口名
搜索 Prompt 列表

### 2. 方法类型
`POST`

### 3. 方法路径
`/api/prompt/search`

### 4. 详细职责
该接口用于 Prompt 治理后台的主列表检索。当前端需要按分类、子分类、key、名称、内容、组装模式、是否启用、是否有模板变量等条件筛选 Prompt 时，应调用这个接口。它支持分页入参，但回参并不是统一分页对象，而是“当前页切片后的数组”；因此前端如果需要总数、总页数，源码本身没有直接提供，页面上只能做“下一页/是否还有更多”之类的弱分页，或者自行根据业务改造。

### 5. 是否鉴权
是。
需要 `Authorization` 请求头中的有效 JWT。当前源码未发现额外角色权限控制。

### 6. 入参字段和类型
- 请求体为 JSON 对象，可为空；为空时按默认检索条件执行。
- `category`：`String`，非必填，一级分类键；与 `categoryKey` 二选一时，源码优先使用 `category`。
- `categoryKey`：`String`，非必填，一级分类键兼容字段。
- `subCategory`：`String`，非必填，二级分类键。
- `keyLike`：`String`，非必填，按 key 模糊包含匹配。
- `nameLike`：`String`，非必填，按名称模糊包含匹配。
- `valueLike`：`String`，非必填，按 Prompt 内容模糊包含匹配。
- `hasTemplateVariables`：`Boolean`，非必填，筛选是否包含模板变量。
- `keywordMatchEnabled`：`Boolean`，非必填，筛选是否启用关键词匹配。
- `assemblyMode`：`String`，非必填，按组装模式精确筛选。
- `enabled`：`Boolean`，非必填，按启用状态筛选。
- `includeDisabled`：`Boolean`，非必填，是否把 disabled 项一起纳入搜索。
- `pageNo`：`Long`，非必填，页码，默认值 `1`，小于等于 `0` 时按 `1` 处理。
- `pageSize`：`Long`，非必填，每页条数，默认值 `20`，最大值 `200`。
- 分页说明：源码只做数组切片，不返回 `total`、`pages` 等分页元数据。
- 排序说明：源码按注册视图当前顺序过滤后再切片；前端不能自定义排序字段。

### 7. 回参字段和类型
- 成功时返回 `Array<PromptItemRecord>`，没有统一分页包装层。
- `[].itemId`：`Long`，条目 ID。
- `[].versionId`：`Long`，当前展示版本 ID。
- `[].key`：`String`，Prompt 唯一键。
- `[].name`：`String`，Prompt 名称。
- `[].value`：`String`，Prompt 正文。
- `[].category`：`String`，一级分类键。
- `[].subCategory`：`String`，二级分类键。
- `[].description`：`String`，描述。
- `[].runtimeSlot`：`String`，运行时槽位。
- `[].hasTemplateVariables`：`Boolean`，是否包含模板变量。
- `[].templateVariables`：`Array<String>`，模板变量列表。
- `[].keywordMatchEnabled`：`Boolean`，是否启用关键词匹配。
- `[].matchKeywords`：`Array<String>`，关键词列表。
- `[].assemblyMode`：`String`，组装模式。
- `[].matchScope`：`Object`，匹配范围。
- `[].matchScope.agents`：`Array<String>`。
- `[].matchScope.nodeKinds`：`Array<String>`。
- `[].matchScope.taskStates`：`Array<String>`。
- `[].matchScope.modelFamilies`：`Array<String>`。
- `[].matchScope.personaIds`：`Array<String>`。
- `[].matchScope.sceneIds`：`Array<String>`。
- `[].editPolicy`：`Object`，编辑策略。
- `[].editPolicy.create`：`Boolean`。
- `[].editPolicy.update`：`Boolean`。
- `[].editPolicy.delete`：`Boolean`。
- `[].enabled`：`Boolean`，是否启用。
- `[].priority`：`Integer`，优先级。
- `[].status`：`String`，条目状态。
- `[].version`：`String`，版本号。
- `[].versionLabel`：`String`，版本标签。
- `[].changeNote`：`String`，变更说明。
- 搜索结果为空时返回空数组 `[]`。
- 失败时通常返回文档开头“统一异常与错误返回”中的错误结构。

---

### 1. 接口名
创建 Prompt

### 2. 方法类型
`POST`

### 3. 方法路径
`/api/prompt/item/create`

### 4. 详细职责
该接口用于治理后台新建一条 Prompt 配置。前端通常在“新建 Prompt”表单提交时调用它，用来创建新的内容类 Prompt，并同步落表生成第一版生效版本。调用成功后，前端可以直接得到创建后的完整 Prompt 记录，用于刷新列表、跳转详情页或继续编辑。需要注意的是，源码明确限制该接口只允许创建内容类 Prompt，不支持直接创建执行类 Prompt，也不允许在内容类创建时携带模板变量占位逻辑。

### 5. 是否鉴权
是。
需要 `Authorization` 请求头中的有效 JWT。当前源码未发现额外角色权限控制。

### 6. 入参字段和类型
- 请求体为 `PromptUpsertRequest`，不能为空。
- `key`：`String`，必填，Prompt 唯一键；必须匹配 `{category}.{subCategory}.{name}_{versionTag}` 格式，且必须使用规范 key，不能用别名 key。
- `promptName`：`String`，非必填，Prompt 展示名称；不传时默认使用 `key`。
- `value`：`String`，非必填但业务上建议必填，Prompt 正文内容；内容类创建时不能包含 `${...}` 模板占位符。
- `category`：`String`，与 `categoryKey` 至少传一个，一级分类键。
- `categoryKey`：`String`，与 `category` 兼容的一级分类键字段；若同时传，源码优先使用 `categoryKey`。
- `subCategory`：`String`，必填，二级分类键；必须与 `key` 中第二段保持一致。
- `description`：`String`，非必填，描述说明。
- `runtimeSlot`：`String`，非必填，运行时槽位；如传入，必须属于以下允许值之一：`instructions.system`、`instructions.persona`、`instructions.scene`、`memory.hints`、`knowledge.evidence`、`output.constraints`、`runtime.prompt`、`agent.reconstruction`、`agent.summary`、`repair.main`、`agent.exception_analysis`、`repair.exception`、`agent.rerank`、`agent.recovery`、`agent.tool_semantic`、`agent.tool_args`、`agent.workflow_args`、`agent.tool_args_repair`、`agent.tool_decision`、`agent.master_planning`、`task.plan.final_result_to_luna`、`rag.planner.query`、`rag.planner.source_process`、`rag.planner.agent_stage`、`rag.planner.global_rerank`。
- `hasTemplateVariables`：`Boolean`，非必填；创建内容类 Prompt 时必须为 `false` 或不传，传 `true` 会报错。
- `templateVariables`：`Array<String>`，非必填；创建内容类 Prompt 时禁止传值。
- `keywordMatchEnabled`：`Boolean`，非必填；默认值通常为 `true`，但如果分类本身不允许关键词匹配，源码会强制写成 `false`。
- `matchKeywords`：`Array<String>`，非必填，关键词列表。
- `assemblyMode`：`String`，非必填，内容类允许值为 `ALWAYS`、`KEYWORD_ONLY`、`KEYWORD_AND_AGENT`、`KEYWORD_OR_AGENT`、`DISABLED`；默认值 `KEYWORD_ONLY`。
- `matchScope`：`Object`，非必填，匹配范围。
- `matchScope.agents`：`Array<String>`，非必填。
- `matchScope.nodeKinds`：`Array<String>`，非必填。
- `matchScope.taskStates`：`Array<String>`，非必填。
- `matchScope.modelFamilies`：`Array<String>`，非必填。
- `matchScope.personaIds`：`Array<String>`，非必填。
- `matchScope.sceneIds`：`Array<String>`，非必填。
- `editPolicy`：`Object`，非必填，编辑策略。
- `editPolicy.create`：`Boolean`，非必填；默认内容类策略为 `true`。
- `editPolicy.update`：`Boolean`，非必填；默认内容类策略为 `true`。
- `editPolicy.delete`：`Boolean`，非必填；默认内容类策略为 `true`。
- `enabled`：`Boolean`，非必填，条目是否启用；默认值 `true`，除非显式传 `false`。
- `priority`：`Integer`，非必填，优先级；默认值 `80`。
- `status`：`String`，非必填。源码同时把这个字段用于条目状态和版本状态，条目层只识别 `enabled`/`disabled`，版本层只识别 `draft`/`active`/`archived`；混用时行为并不直观，前端应谨慎使用。未传时条目默认 `enabled`，版本默认 `active`。
- `version`：`String`，非必填，版本号；默认值 `1.0.0`。
- `versionLabel`：`String`，非必填，版本标签；默认取 `version`，若 `version` 也未传则为空字符串。
- `changeNote`：`String`，非必填，变更说明。
- `previewOnly`：`Boolean`，非必填；创建接口源码未读取该字段，传入也不会触发“只预览不生效”逻辑。
- 额外限制：分类必须存在于 `prompt_category` 表，且不能是执行类分类；`key` 中的分类和二级分类前缀必须与 `category/categoryKey`、`subCategory` 一致。

### 7. 回参字段和类型
- 成功时返回 `PromptItemRecord`。
- `itemId`：`Long`，新创建条目 ID。
- `versionId`：`Long`，首个生效版本 ID。
- `key`：`String`，Prompt 唯一键。
- `name`：`String`，Prompt 名称。
- `value`：`String`，Prompt 正文。
- `category`：`String`，一级分类键。
- `subCategory`：`String`，二级分类键。
- `description`：`String`，描述。
- `runtimeSlot`：`String`，运行时槽位。
- `hasTemplateVariables`：`Boolean`，创建内容类时固定为 `false`。
- `templateVariables`：`Array<String>`，创建内容类时通常为空数组。
- `keywordMatchEnabled`：`Boolean`，关键词匹配是否开启。
- `matchKeywords`：`Array<String>`，关键词列表。
- `assemblyMode`：`String`，组装模式。
- `matchScope`：`Object`。
- `matchScope.agents`：`Array<String>`。
- `matchScope.nodeKinds`：`Array<String>`。
- `matchScope.taskStates`：`Array<String>`。
- `matchScope.modelFamilies`：`Array<String>`。
- `matchScope.personaIds`：`Array<String>`。
- `matchScope.sceneIds`：`Array<String>`。
- `editPolicy`：`Object`。
- `editPolicy.create`：`Boolean`。
- `editPolicy.update`：`Boolean`。
- `editPolicy.delete`：`Boolean`。
- `enabled`：`Boolean`。
- `priority`：`Integer`。
- `status`：`String`。
- `version`：`String`。
- `versionLabel`：`String`。
- `changeNote`：`String`。
- 常见失败原因会走统一异常返回，例如 `request is required`、`key is required`、`prompt key already exists`、`category must exist in prompt_category`、`create only supports content prompt category`。

---

### 1. 接口名
更新 Prompt

### 2. 方法类型
`POST`

### 3. 方法路径
`/api/prompt/item/update`

### 4. 详细职责
该接口用于治理后台编辑已存在的 Prompt。前端在详情页、编辑页点击保存时，通常调用这个接口。它会先更新条目层元数据，再新建一个新的版本记录；如果是普通更新，新的版本会立即被激活并成为当前生效版本；如果是执行类 Prompt 且 `previewOnly=true`，则只保存草稿版本，不切换当前生效版本。也就是说，前端需要区分“正式保存”和“执行类草稿预览保存”两种联调方式。

### 5. 是否鉴权
是。
需要 `Authorization` 请求头中的有效 JWT。当前源码未发现额外角色权限控制。

### 6. 入参字段和类型
- 请求体为 `PromptUpsertRequest`。
- `key`：`String`，必填，要更新的 Prompt 唯一键；必须先存在，否则返回 `prompt not found`。
- `promptName`：`String`，非必填，Prompt 名称；传了才更新。
- `value`：`String`，非必填，Prompt 正文；传了才写入新版本。
- `category`：`String`，非必填，一级分类键。
- `categoryKey`：`String`，非必填，与 `category` 兼容；若传入则优先使用。
- `subCategory`：`String`，非必填，二级分类键。
- `description`：`String`，非必填。
- `runtimeSlot`：`String`，非必填；如果修改该值，必须属于允许槽位词表。
- `hasTemplateVariables`：`Boolean`，非必填。内容类 Prompt 更新时不能传 `true`；执行类 Prompt 场景下源码不会用这个字段切换类型。
- `templateVariables`：`Array<String>`，非必填。执行类 Prompt 可传，变量名必须匹配 Java 风格标识符；内容类 Prompt 禁止传值。
- `keywordMatchEnabled`：`Boolean`，非必填。执行类 Prompt 不允许传 `true`；内容类 Prompt 可用。
- `matchKeywords`：`Array<String>`，非必填。
- `assemblyMode`：`String`，非必填。内容类允许 `ALWAYS`、`KEYWORD_ONLY`、`KEYWORD_AND_AGENT`、`KEYWORD_OR_AGENT`、`DISABLED`；执行类允许 `ALWAYS`、`AGENT_ONLY`、`POLICY_ONLY`、`MANUAL_ONLY`、`DISABLED`。
- `matchScope`：`Object`，非必填。
- `matchScope.agents`：`Array<String>`，非必填。
- `matchScope.nodeKinds`：`Array<String>`，非必填。
- `matchScope.taskStates`：`Array<String>`，非必填。
- `matchScope.modelFamilies`：`Array<String>`，非必填。
- `matchScope.personaIds`：`Array<String>`，非必填。
- `matchScope.sceneIds`：`Array<String>`，非必填。
- `editPolicy`：`Object`，非必填；若不传则继承当前版本的编辑策略。
- `editPolicy.create`：`Boolean`，非必填。
- `editPolicy.update`：`Boolean`，非必填；若当前版本策略明确不允许更新，则接口直接报错。
- `editPolicy.delete`：`Boolean`，非必填。
- `enabled`：`Boolean`，非必填。内容类 Prompt 可改；执行类 Prompt 不允许改成 `false`。
- `priority`：`Integer`，非必填。
- `status`：`String`，非必填。内容类条目常见值 `enabled`/`disabled`；执行类条目不允许设为 `disabled`。版本层常见值 `draft`/`active`/`archived`，源码仍复用同一字段。
- `version`：`String`，非必填；不传时自动生成下一个补丁版本号，例如从 `1.0.0` 递增到 `1.0.1`。
- `versionLabel`：`String`，非必填。
- `changeNote`：`String`，非必填。
- `previewOnly`：`Boolean`，非必填。只有“执行类 Prompt”场景下传 `true` 才会生效，此时接口保存草稿版本但不会激活。
- 类型边界限制：
- 内容类 Prompt 不能迁移到执行类分类。
- 执行类 Prompt 不能迁移到内容类分类。
- 如果 `key` 是规范主 key，则 `category/subCategory` 仍必须与 key 前缀一致。

### 7. 回参字段和类型
- 成功时返回 `PromptItemRecord`。
- 返回字段与详情接口一致：
- `itemId`：`Long`。
- `versionId`：`Long`。
- `key`：`String`。
- `name`：`String`。
- `value`：`String`。
- `category`：`String`。
- `subCategory`：`String`。
- `description`：`String`。
- `runtimeSlot`：`String`。
- `hasTemplateVariables`：`Boolean`。
- `templateVariables`：`Array<String>`。
- `keywordMatchEnabled`：`Boolean`。
- `matchKeywords`：`Array<String>`。
- `assemblyMode`：`String`。
- `matchScope`：`Object`。
- `matchScope.agents`：`Array<String>`。
- `matchScope.nodeKinds`：`Array<String>`。
- `matchScope.taskStates`：`Array<String>`。
- `matchScope.modelFamilies`：`Array<String>`。
- `matchScope.personaIds`：`Array<String>`。
- `matchScope.sceneIds`：`Array<String>`。
- `editPolicy`：`Object`。
- `editPolicy.create`：`Boolean`。
- `editPolicy.update`：`Boolean`。
- `editPolicy.delete`：`Boolean`。
- `enabled`：`Boolean`。
- `priority`：`Integer`。
- `status`：`String`。
- `version`：`String`。
- `versionLabel`：`String`。
- `changeNote`：`String`。
- 重要说明：如果是执行类 Prompt 且 `previewOnly=true`，源码会新建 draft 版本但不会切换当前生效版本，因此这里返回的仍然是当前生效的 `PromptItemRecord`，不是新草稿的详情。
- 常见失败原因会走统一异常返回，例如 `prompt not found`、`prompt update policy denied`、`content prompt update cannot carry templateVariables`、`execution prompt cannot enable keyword matching`、`execution prompt cannot be disabled`。

---

### 1. 接口名
保存 Prompt（存在则更新，不存在则创建）

### 2. 方法类型
`POST`

### 3. 方法路径
`/api/prompt/item/save`

### 4. 详细职责
该接口是前端统一保存入口，适合页面不想区分“新建”和“编辑”时直接调用。源码会先根据 `key` 判断是否已存在：存在则走更新流程，不存在则走创建流程。前端在做“统一保存按钮”的治理页面时可以直接对接这个接口，但需要注意它的存在性判断使用的是注册视图而不仅是数据库表，因此某些 builtin 场景下会先判定为存在，再进入更新逻辑。

### 5. 是否鉴权
是。
需要 `Authorization` 请求头中的有效 JWT。当前源码未发现额外角色权限控制。

### 6. 入参字段和类型
- 请求体结构与 `/api/prompt/item/create`、`/api/prompt/item/update` 相同，使用 `PromptUpsertRequest`。
- `key`：`String`，必填；缺失时直接报错 `key is required`。
- 其余字段与创建/更新接口完全一致，含义也一致：
- `promptName`：`String`，非必填。
- `value`：`String`，非必填。
- `category`：`String`，非必填。
- `categoryKey`：`String`，非必填。
- `subCategory`：`String`，非必填。
- `description`：`String`，非必填。
- `runtimeSlot`：`String`，非必填。
- `hasTemplateVariables`：`Boolean`，非必填。
- `templateVariables`：`Array<String>`，非必填。
- `keywordMatchEnabled`：`Boolean`，非必填。
- `matchKeywords`：`Array<String>`，非必填。
- `assemblyMode`：`String`，非必填。
- `matchScope`：`Object`，非必填。
- `matchScope.agents`：`Array<String>`，非必填。
- `matchScope.nodeKinds`：`Array<String>`，非必填。
- `matchScope.taskStates`：`Array<String>`，非必填。
- `matchScope.modelFamilies`：`Array<String>`，非必填。
- `matchScope.personaIds`：`Array<String>`，非必填。
- `matchScope.sceneIds`：`Array<String>`，非必填。
- `editPolicy`：`Object`，非必填。
- `editPolicy.create`：`Boolean`，非必填。
- `editPolicy.update`：`Boolean`，非必填。
- `editPolicy.delete`：`Boolean`，非必填。
- `enabled`：`Boolean`，非必填。
- `priority`：`Integer`，非必填。
- `status`：`String`，非必填。
- `version`：`String`，非必填。
- `versionLabel`：`String`，非必填。
- `changeNote`：`String`，非必填。
- `previewOnly`：`Boolean`，非必填。
- 分支说明：如果判断为“已存在”，则完整套用更新接口的限制；否则套用创建接口的限制。

### 7. 回参字段和类型
- 成功时返回 `PromptItemRecord`。
- 返回字段与详情接口一致：
- `itemId`：`Long`。
- `versionId`：`Long`。
- `key`：`String`。
- `name`：`String`。
- `value`：`String`。
- `category`：`String`。
- `subCategory`：`String`。
- `description`：`String`。
- `runtimeSlot`：`String`。
- `hasTemplateVariables`：`Boolean`。
- `templateVariables`：`Array<String>`。
- `keywordMatchEnabled`：`Boolean`。
- `matchKeywords`：`Array<String>`。
- `assemblyMode`：`String`。
- `matchScope`：`Object`。
- `matchScope.agents`：`Array<String>`。
- `matchScope.nodeKinds`：`Array<String>`。
- `matchScope.taskStates`：`Array<String>`。
- `matchScope.modelFamilies`：`Array<String>`。
- `matchScope.personaIds`：`Array<String>`。
- `matchScope.sceneIds`：`Array<String>`。
- `editPolicy`：`Object`。
- `editPolicy.create`：`Boolean`。
- `editPolicy.update`：`Boolean`。
- `editPolicy.delete`：`Boolean`。
- `enabled`：`Boolean`。
- `priority`：`Integer`。
- `status`：`String`。
- `version`：`String`。
- `versionLabel`：`String`。
- `changeNote`：`String`。
- 失败时通常返回统一异常结构；若仅命中 builtin fallback 而数据库中没有对应条目，源码存在先判定存在、再更新时报 `prompt not found` 的可能。

---

### 1. 接口名
删除 Prompt

### 2. 方法类型
`POST`

### 3. 方法路径
`/api/prompt/item/delete`

### 4. 详细职责
该接口用于治理后台显式删除 Prompt 条目。这里的“删除”不是物理删除数据库记录，而是软删除：把条目标记为禁用并保留历史版本。前端在列表页执行删除确认、详情页点击删除时调用它即可。因为源码保留历史数据，所以删除后仍可以通过版本相关接口查到历史版本。

### 5. 是否鉴权
是。
需要 `Authorization` 请求头中的有效 JWT。当前源码未发现额外角色权限控制。

### 6. 入参字段和类型
- 请求体为 JSON 对象。
- `key`：`String`，必填，要删除的 Prompt 唯一键。
- 删除限制：
- 执行类分类下的 Prompt 不能删除。
- 执行类 Prompt 不能删除。
- 当前版本的 `editPolicy.delete` 不为 `true` 时不能删除。

### 7. 回参字段和类型
- 成功时返回 `Object`。
- `success`：`Boolean`，固定为 `true`。
- `key`：`String`，本次删除的 key。
- 重要说明：如果数据库中没有该 key，源码也会直接返回成功，不会报“未找到”。
- 删除后条目会被置为 `enabled=false`、`status=disabled`，但这些变更不会在返回体中展开。
- 失败时通常返回统一异常结构，例如 `key is required`、`execution prompt category cannot be deleted`、`prompt delete policy denied`。

---

### 1. 接口名
查询 Prompt 版本列表

### 2. 方法类型
`GET`

### 3. 方法路径
`/api/prompt/item/versions`

### 4. 详细职责
该接口用于版本管理页、版本抽屉、历史版本回看场景。当前端需要展示某个 Prompt 的所有历史版本、草稿版本、归档版本，并允许用户后续执行激活、回滚、归档或对比时，应先调用这个接口。它返回的版本列表按创建时间倒序排列，前端可以直接做“最新在前”的版本时间线。

### 5. 是否鉴权
是。
需要 `Authorization` 请求头中的有效 JWT。当前源码未发现额外角色权限控制。

### 6. 入参字段和类型
- `key`：`String`，必填，Prompt 唯一键。

### 7. 回参字段和类型
- 成功时返回 `Array<PromptItemVersionEntity>`。
- `[].id`：`Long`，版本 ID。
- `[].promptItemId`：`Long`，所属 Prompt 条目 ID。
- `[].versionNo`：`String`，版本号。
- `[].versionLabel`：`String`，版本标签。
- `[].promptValue`：`String`，该版本的 Prompt 内容。
- `[].templateVariables`：`Array<String>`，模板变量列表。
- `[].matchKeywords`：`Array<String>`，关键词列表。
- `[].matchScope`：`Object`，匹配范围配置。
- `[].matchScope.agents`：`Array<String>`，若存在则表示代理限制。
- `[].matchScope.nodeKinds`：`Array<String>`。
- `[].matchScope.taskStates`：`Array<String>`。
- `[].matchScope.modelFamilies`：`Array<String>`。
- `[].matchScope.personaIds`：`Array<String>`。
- `[].matchScope.sceneIds`：`Array<String>`。
- `[].editPolicy`：`Object`，编辑策略配置。
- `[].editPolicy.create`：`Boolean`。
- `[].editPolicy.update`：`Boolean`。
- `[].editPolicy.delete`：`Boolean`。
- `[].status`：`String`，版本状态，常见值包括 `draft`、`active`、`archived`。
- `[].changeNote`：`String`，版本变更说明。
- `[].isActive`：`Boolean`，是否当前激活版本。
- `[].createdAt`：`String`，创建时间。
- `[].updatedAt`：`String`，更新时间。
- 返回为空数组 `[]` 的情况包括：key 为空、Prompt 不存在、该 Prompt 没有版本记录。

---

### 1. 接口名
查询 Prompt 单个版本详情

### 2. 方法类型
`GET`

### 3. 方法路径
`/api/prompt/item/version/detail`

### 4. 详细职责
该接口用于版本详情查看、版本对比前先取详情、查看某个草稿版本完整内容等场景。当前端已经拿到 `versionId`，希望直接读取该版本正文、关键词、匹配范围、编辑策略等完整字段时，可以调用这个接口。它适合从版本列表跳详情，也适合弹窗查看具体版本内容。

### 5. 是否鉴权
是。
需要 `Authorization` 请求头中的有效 JWT。当前源码未发现额外角色权限控制。

### 6. 入参字段和类型
- `versionId`：`Long`，必填，版本 ID。

### 7. 回参字段和类型
- 成功时返回 `PromptItemVersionEntity`；未命中时返回空对象 `{}`。
- `id`：`Long`，版本 ID。
- `promptItemId`：`Long`，所属 Prompt 条目 ID。
- `versionNo`：`String`，版本号。
- `versionLabel`：`String`，版本标签。
- `promptValue`：`String`，版本内容。
- `templateVariables`：`Array<String>`，模板变量列表。
- `matchKeywords`：`Array<String>`，关键词列表。
- `matchScope`：`Object`，匹配范围配置。
- `matchScope.agents`：`Array<String>`。
- `matchScope.nodeKinds`：`Array<String>`。
- `matchScope.taskStates`：`Array<String>`。
- `matchScope.modelFamilies`：`Array<String>`。
- `matchScope.personaIds`：`Array<String>`。
- `matchScope.sceneIds`：`Array<String>`。
- `editPolicy`：`Object`。
- `editPolicy.create`：`Boolean`。
- `editPolicy.update`：`Boolean`。
- `editPolicy.delete`：`Boolean`。
- `status`：`String`，版本状态。
- `changeNote`：`String`，变更说明。
- `isActive`：`Boolean`，是否当前激活。
- `createdAt`：`String`，创建时间。
- `updatedAt`：`String`，更新时间。

---

### 1. 接口名
激活 Prompt 版本

### 2. 方法类型
`POST`

### 3. 方法路径
`/api/prompt/item/activate`

### 4. 详细职责
该接口用于把某个历史版本切换为当前生效版本。前端在版本列表点击“设为当前版本”“激活版本”时调用它。调用后，源码会把同一 Prompt 当前已激活版本归档，再把目标版本置为激活，同时把条目本身设置成启用状态。适合做正式版本切换，而不是草稿保存。

### 5. 是否鉴权
是。
需要 `Authorization` 请求头中的有效 JWT。当前源码未发现额外角色权限控制。

### 6. 入参字段和类型
- 请求体为 `PromptVersionSwitchRequest`。
- `versionId`：`Long`，必填，要激活的版本 ID。
- `key`：`String`，非必填；当前控制器不会使用该字段，即使传了也不会参与校验。

### 7. 回参字段和类型
- 成功时返回 `Object`。
- `success`：`Boolean`，固定为 `true`。
- `versionId`：`Long`，被激活的版本 ID。
- 激活成功后，条目层会被同步置为 `enabled=true`、`status=enabled`，但这些字段不在响应体中展开。
- 失败时通常返回统一异常结构，例如 `versionId is required`、`version not found`。

---

### 1. 接口名
回滚 Prompt 到指定版本

### 2. 方法类型
`POST`

### 3. 方法路径
`/api/prompt/item/rollback`

### 4. 详细职责
该接口用于“按指定 key 回滚到某个历史版本”。前端在版本管理页点击“回滚”时应调用它，而不是直接调用激活接口，因为这里会额外校验目标版本确实属于当前 Prompt。它适合做安全回退，防止前端误把别的 Prompt 的版本 ID 传过来。

### 5. 是否鉴权
是。
需要 `Authorization` 请求头中的有效 JWT。当前源码未发现额外角色权限控制。

### 6. 入参字段和类型
- 请求体为 `PromptVersionSwitchRequest`。
- `versionId`：`Long`，必填，要回滚到的版本 ID。
- `key`：`String`，必填，当前 Prompt 唯一键。

### 7. 回参字段和类型
- 成功时返回 `Object`。
- `success`：`Boolean`，固定为 `true`。
- `versionId`：`Long`，目标版本 ID。
- `key`：`String`，本次回滚的 Prompt key。
- 回滚成功后的底层效果与“激活版本”一致：旧活动版本会被归档，目标版本成为当前活动版本。
- 失败时通常返回统一异常结构，例如 `key is required`、`versionId is required`、`prompt not found`、`version not found`、`version does not belong to key`。

---

### 1. 接口名
保存 Prompt 草稿版本

### 2. 方法类型
`POST`

### 3. 方法路径
`/api/prompt/item/draft`

### 4. 详细职责
该接口用于把当前编辑内容暂存为草稿版本，而不影响线上生效版本。前端在治理后台提供“保存草稿”“暂存修改”能力时，应对接这个接口。与更新接口的 `previewOnly` 不同，这个接口会直接返回新建的草稿版本实体，方便前端马上刷新版本列表或跳转草稿详情。

### 5. 是否鉴权
是。
需要 `Authorization` 请求头中的有效 JWT。当前源码未发现额外角色权限控制。

### 6. 入参字段和类型
- 请求体为 `PromptUpsertRequest`。
- `key`：`String`，必填，要保存草稿的 Prompt 唯一键；不存在会报 `prompt not found`。
- `promptName`：`String`，非必填；源码不会把它写入草稿版本实体。
- `value`：`String`，非必填；不传时继承最新版本的内容。
- `category`：`String`，非必填；源码不会使用。
- `categoryKey`：`String`，非必填；源码不会使用。
- `subCategory`：`String`，非必填；源码不会使用。
- `description`：`String`，非必填；源码不会使用。
- `runtimeSlot`：`String`，非必填；源码不会使用。
- `hasTemplateVariables`：`Boolean`，非必填；源码不会使用。
- `templateVariables`：`Array<String>`，非必填；不传时继承最新版本。
- `keywordMatchEnabled`：`Boolean`，非必填；源码不会使用。
- `matchKeywords`：`Array<String>`，非必填；不传时继承最新版本。
- `assemblyMode`：`String`，非必填；源码不会使用。
- `matchScope`：`Object`，非必填；不传时继承最新版本。
- `matchScope.agents`：`Array<String>`，非必填。
- `matchScope.nodeKinds`：`Array<String>`，非必填。
- `matchScope.taskStates`：`Array<String>`，非必填。
- `matchScope.modelFamilies`：`Array<String>`，非必填。
- `matchScope.personaIds`：`Array<String>`，非必填。
- `matchScope.sceneIds`：`Array<String>`，非必填。
- `editPolicy`：`Object`，非必填；不传时继承最新版本。
- `editPolicy.create`：`Boolean`，非必填。
- `editPolicy.update`：`Boolean`，非必填。
- `editPolicy.delete`：`Boolean`，非必填。
- `enabled`：`Boolean`，非必填；源码不会使用。
- `priority`：`Integer`，非必填；源码不会使用。
- `status`：`String`，非必填；源码不会读取，草稿状态固定写为 `draft`。
- `version`：`String`，非必填；源码不会使用，草稿版本号自动递增。
- `versionLabel`：`String`，非必填；默认值 `draft`。
- `changeNote`：`String`，非必填；默认值 `draft_save`。
- `previewOnly`：`Boolean`，非必填；源码不会使用。

### 7. 回参字段和类型
- 成功时返回 `PromptItemVersionEntity`，表示新生成的草稿版本。
- `id`：`Long`，草稿版本 ID。
- `promptItemId`：`Long`，所属 Prompt 条目 ID。
- `versionNo`：`String`，自动递增后的版本号。
- `versionLabel`：`String`，版本标签；未传时为 `draft`。
- `promptValue`：`String`，草稿内容。
- `templateVariables`：`Array<String>`，模板变量列表。
- `matchKeywords`：`Array<String>`，关键词列表。
- `matchScope`：`Object`。
- `matchScope.agents`：`Array<String>`。
- `matchScope.nodeKinds`：`Array<String>`。
- `matchScope.taskStates`：`Array<String>`。
- `matchScope.modelFamilies`：`Array<String>`。
- `matchScope.personaIds`：`Array<String>`。
- `matchScope.sceneIds`：`Array<String>`。
- `editPolicy`：`Object`。
- `editPolicy.create`：`Boolean`。
- `editPolicy.update`：`Boolean`。
- `editPolicy.delete`：`Boolean`。
- `status`：`String`，固定为 `draft`。
- `changeNote`：`String`，默认 `draft_save`，也可能是请求传入值。
- `isActive`：`Boolean`，固定为 `false`。
- `createdAt`：`String`，创建时间。
- `updatedAt`：`String`，更新时间。

---

### 1. 接口名
归档 Prompt 版本

### 2. 方法类型
`POST`

### 3. 方法路径
`/api/prompt/item/archive`

### 4. 详细职责
该接口用于把某个版本从可用版本流转为归档状态。前端在版本管理页点击“归档”时调用它。对于非当前版本，它只是把该版本标记为归档；如果归档的是当前活动版本，源码还会把条目本身置为禁用并清空当前版本引用。但执行类 Prompt 的当前活动版本受到保护，不能被直接归档。

### 5. 是否鉴权
是。
需要 `Authorization` 请求头中的有效 JWT。当前源码未发现额外角色权限控制。

### 6. 入参字段和类型
- 请求体为 `PromptVersionSwitchRequest`。
- `versionId`：`Long`，必填，要归档的版本 ID。
- `key`：`String`，非必填；当前控制器不会使用该字段。

### 7. 回参字段和类型
- 成功时返回 `Object`。
- `success`：`Boolean`，固定为 `true`。
- `versionId`：`Long`，被归档的版本 ID。
- 如果被归档的是当前活动版本且不是执行类 Prompt，条目层会被同步设为 `currentVersionId=null`、`enabled=false`、`status=disabled`，但这些字段不在响应体中展开。
- 失败时通常返回统一异常结构，例如 `versionId is required`、`version not found`、`execution prompt current version cannot be archived`。

---

### 1. 接口名
对比 Prompt 两个版本差异

### 2. 方法类型
`POST`

### 3. 方法路径
`/api/prompt/item/diff`

### 4. 详细职责
该接口用于版本对比弹窗、差异审查页、回滚前确认场景。前端传入左右两个版本 ID 后，后端会按行比较两个版本的 Prompt 文本，并生成简单 diff 结果。适合做“我这次改了什么”的展示，不适合做富文本级或词级高亮比对，因为源码只返回按行拼好的差异列表。

### 5. 是否鉴权
是。
需要 `Authorization` 请求头中的有效 JWT。当前源码未发现额外角色权限控制。

### 6. 入参字段和类型
- 请求体为 `PromptVersionDiffRequest`，不能为空。
- `leftVersionId`：`Long`，必填，左侧版本 ID。
- `rightVersionId`：`Long`，必填，右侧版本 ID。
- 限制：两个版本必须都存在，并且必须属于同一个 Prompt 条目，否则会报错。

### 7. 回参字段和类型
- 成功时返回 `Object`。
- `leftVersionId`：`Long`，左侧版本 ID。
- `rightVersionId`：`Long`，右侧版本 ID。
- `leftVersionNo`：`String`，左侧版本号。
- `rightVersionNo`：`String`，右侧版本号。
- `changed`：`Boolean`，是否存在差异。
- `diffLines`：`Array<String>`，按行输出的差异内容。
- `diffLines[]`：`String`，以 `- ` 开头表示左侧被删除的行，以 `+ ` 开头表示右侧新增的行。
- 如果两个版本完全相同，则 `changed=false`，`diffLines=[]`。
- 失败时通常返回统一异常结构，例如 `request is required`、`version ids are required`、`version not found`、`versions are not from same prompt item`。

---

### 1. 接口名
预览 Prompt 匹配结果

### 2. 方法类型
`POST`

### 3. 方法路径
`/api/prompt/preview/match`

### 4. 详细职责
该接口用于在不真正进入主业务链路的情况下，模拟某段上下文会命中哪些 Prompt。前端在 Prompt 治理后台做匹配调试、规则验证、手工指定 Prompt 试跑时，应调用它。它会返回“哪些 Prompt 被命中”“哪些 Prompt 被拒绝以及原因”，因此非常适合联调规则是否生效、策略包是否覆盖正确、人工指定 Prompt 是否进入结果集。

### 5. 是否鉴权
是。
需要 `Authorization` 请求头中的有效 JWT。当前源码未发现额外角色权限控制。

### 6. 入参字段和类型
- 请求体为 `PromptPreviewRequest`，前端应传 JSON 对象；源码未对字段做必填校验。
- `sessionId`：`String`，非必填，会话 ID，用于模拟某个会话上下文。
- `userInput`：`String`，非必填，用户输入文本。
- `policyId`：`String`，非必填，要模拟应用的策略包 ID。
- `personaId`：`String`，非必填，人格标识。
- `sceneId`：`String`，非必填，场景标识。
- `agent`：`String`，非必填，代理或执行主体标识。
- `nodeKind`：`String`，非必填，节点类型。
- `taskState`：`String`，非必填，任务状态。
- `modelFamily`：`String`，非必填，模型家族。
- `manualPromptKeys`：`Array<String>`，非必填，手动指定要参与解析的 Prompt key 列表。

### 7. 回参字段和类型
- 成功时返回 `Object`。
- `policyId`：`String`，本次实际参与预览的策略 ID；源码未解析到时可能为空字符串。
- `matchedItems`：`Array<Object>`，命中的 Prompt 列表。
- `matchedItems[].itemId`：`Long`，条目 ID。
- `matchedItems[].versionId`：`Long`，版本 ID。
- `matchedItems[].key`：`String`，Prompt key。
- `matchedItems[].name`：`String`，Prompt 名称。
- `matchedItems[].value`：`String`，Prompt 内容。
- `matchedItems[].category`：`String`，一级分类键。
- `matchedItems[].subCategory`：`String`，二级分类键。
- `matchedItems[].description`：`String`，描述。
- `matchedItems[].runtimeSlot`：`String`，运行时槽位。
- `matchedItems[].assemblyMode`：`String`，组装模式。
- `matchedItems[].matchReason`：`String`，命中原因。
- `matchedItems[].reason`：`String`，与 `matchReason` 含义相同的别名字段。
- `matchedItems[].policyApplied`：`Boolean`，是否经过策略包显式干预命中。
- `matchedItems[].hasTemplateVariables`：`Boolean`，是否包含模板变量。
- `matchedItems[].keywordMatchEnabled`：`Boolean`，是否启用关键词匹配。
- `matchedItems[].priority`：`Integer`，优先级。
- `matchedItems[].version`：`String`，版本号。
- `matchedItems[].versionLabel`：`String`，版本标签。
- `matchedItems[].assemblerVersion`：`String`，组装器版本号。
- `rejectedItems`：`Array<RejectedPromptItem>`，被拒绝的 Prompt 列表。
- `rejectedItems[].key`：`String`，被拒绝的 Prompt key。
- `rejectedItems[].rejectedReason`：`String`，被拒绝原因。
- 未命中时，`matchedItems` 和 `rejectedItems` 都可能返回空数组。

---

### 1. 接口名
预览 Prompt 组装结果

### 2. 方法类型
`POST`

### 3. 方法路径
`/api/prompt/preview/assemble`

### 4. 详细职责
该接口用于进一步模拟“命中 Prompt 之后，最终各个运行时槽位会被组装成什么文本”。前端在治理后台做最终 Prompt 预览、分槽位审查、检查哪些 Prompt 在最终 section 过滤中被剔除时，应调用它。和 `/api/prompt/preview/match` 相比，它不仅告诉前端“谁命中了”，还会把命中结果按运行时槽位和 section 进行分层组装，非常适合做最终出参预览页。

### 5. 是否鉴权
是。
需要 `Authorization` 请求头中的有效 JWT。当前源码未发现额外角色权限控制。

### 6. 入参字段和类型
- 请求体结构与 `/api/prompt/preview/match` 相同，使用 `PromptPreviewRequest`。
- `sessionId`：`String`，非必填。
- `userInput`：`String`，非必填。
- `policyId`：`String`，非必填。
- `personaId`：`String`，非必填。
- `sceneId`：`String`，非必填。
- `agent`：`String`，非必填。
- `nodeKind`：`String`，非必填。
- `taskState`：`String`，非必填。
- `modelFamily`：`String`，非必填。
- `manualPromptKeys`：`Array<String>`，非必填。

### 7. 回参字段和类型
- 成功时返回 `Object`。
- `policyId`：`String`，实际参与预览的策略 ID。
- `matchedItems`：`Array<Object>`，字段与 `/api/prompt/preview/match` 的 `matchedItems` 完全一致。
- `rejectedItems`：`Array<RejectedPromptItem>`，字段与 `/api/prompt/preview/match` 的 `rejectedItems` 完全一致。
- `slotMapping`：`Object`，按运行时槽位分组的命中 Prompt 列表。
- `slotMapping.{runtimeSlot}`：`Array<ResolvedPromptItem>`，每个槽位下的命中项数组。
- `slotMapping.{runtimeSlot}[].itemId`：`Long`。
- `slotMapping.{runtimeSlot}[].versionId`：`Long`。
- `slotMapping.{runtimeSlot}[].key`：`String`。
- `slotMapping.{runtimeSlot}[].name`：`String`。
- `slotMapping.{runtimeSlot}[].value`：`String`。
- `slotMapping.{runtimeSlot}[].category`：`String`。
- `slotMapping.{runtimeSlot}[].subCategory`：`String`。
- `slotMapping.{runtimeSlot}[].description`：`String`。
- `slotMapping.{runtimeSlot}[].runtimeSlot`：`String`。
- `slotMapping.{runtimeSlot}[].assemblyMode`：`String`。
- `slotMapping.{runtimeSlot}[].matchReason`：`String`。
- `slotMapping.{runtimeSlot}[].policyApplied`：`Boolean`。
- `slotMapping.{runtimeSlot}[].hasTemplateVariables`：`Boolean`。
- `slotMapping.{runtimeSlot}[].keywordMatchEnabled`：`Boolean`。
- `slotMapping.{runtimeSlot}[].priority`：`Integer`。
- `slotMapping.{runtimeSlot}[].version`：`String`。
- `slotMapping.{runtimeSlot}[].versionLabel`：`String`。
- `slotMapping.{runtimeSlot}[].assemblerVersion`：`String`。
- `assembled`：`Object`，按运行时槽位拼接后的最终文本。
- `assembled.{runtimeSlot}`：`String`，该槽位最终组装结果。
- `sectionMapping`：`Object`，按 section 维度分组后的文本片段列表。
- `sectionMapping.{sectionName}`：`Array<String>`，该 section 下的文本片段列表。
- `sectionAssembled`：`Object`，按 section 拼接后的最终文本。
- `sectionAssembled.{sectionName}`：`String`，该 section 的最终组装文本。
- `filteredOutItems`：`Array<Object>`，在最终 section 过滤过程中被剔除的 Prompt 引用。
- `filteredOutItems[].itemId`：`Long`，条目 ID。
- `filteredOutItems[].versionId`：`Long`，版本 ID。
- `filteredOutItems[].key`：`String`，Prompt key。
- `filteredOutItems[].version`：`String`，版本号。
- `filteredOutItems[].runtimeSlot`：`String`，原始运行时槽位。
- `filteredOutItems[].matchReason`：`String`，命中原因。
- `filteredOutItems[].category`：`String`，一级分类。
- `filteredOutItems[].value`：`String`，Prompt 内容。
- `filteredOutItems[].promptItemId`：`Long`，`itemId` 的别名字段。
- `filteredOutItems[].promptItemVersionId`：`Long`，`versionId` 的别名字段。
- `filteredOutItems[].promptKey`：`String`，`key` 的别名字段。
- `filteredOutItems[].promptVersion`：`String`，`version` 的别名字段。
- `filteredOutItems[].filteredReason`：`String`，过滤原因。
- `filteredOutItems[].reason`：`String`，过滤原因别名字段。
- `filteredOutItems[].targetSection`：`String`，被过滤时对应的目标 section；源码未明确说明何时一定返回。
- 复杂返回结构说明：本接口是 Prompt 预览里结构最复杂的接口，前端通常至少需要分层处理 `matchedItems`、`slotMapping`、`assembled`、`sectionAssembled` 和 `filteredOutItems`。

---

### 1. 接口名
查询 Prompt 策略列表

### 2. 方法类型
`GET`

### 3. 方法路径
`/api/prompt/policy/list`

### 4. 详细职责
该接口用于策略包管理列表页初始化。前端在展示所有 Prompt 策略包、提供策略切换入口、进入策略详情页之前，通常先调用这个接口拿到当前所有策略的主记录。它返回的是策略主表信息，不包含具体的包含/排除 Prompt key 列表；如果前端需要版本内容，应继续调用策略详情或策略版本列表接口。

### 5. 是否鉴权
是。
需要 `Authorization` 请求头中的有效 JWT。当前源码未发现额外角色权限控制。

### 6. 入参字段和类型
- 无请求体。
- 无查询参数。

### 7. 回参字段和类型
- 成功时返回 `Array<PromptPolicyEntity>`。
- `[].id`：`Long`，策略主键 ID。
- `[].policyKey`：`String`，策略业务标识。
- `[].policyName`：`String`，策略名称。
- `[].description`：`String`，策略说明。
- `[].enabled`：`Boolean`，是否启用。
- `[].currentVersionId`：`Long`，当前生效版本 ID。
- `[].createdAt`：`String`，创建时间。
- `[].updatedAt`：`String`，更新时间。
- 返回顺序按 `updatedAt desc`。

---

### 1. 接口名
查询 Prompt 策略详情

### 2. 方法类型
`GET`

### 3. 方法路径
`/api/prompt/policy/detail`

### 4. 详细职责
该接口用于策略详情页和策略编辑页回填。当前端已经知道某个策略包 `policyId`，需要展示该策略的名称、描述、当前激活版本号、包含哪些 Prompt、排除哪些 Prompt 时，应调用这个接口。它把策略主记录和当前版本内容合并成一个前端友好的详情视图，适合直接渲染表单。

### 5. 是否鉴权
是。
需要 `Authorization` 请求头中的有效 JWT。当前源码未发现额外角色权限控制。

### 6. 入参字段和类型
- `policyId`：`String`，必填，策略业务标识。

### 7. 回参字段和类型
- 成功时返回 `PromptPolicyDetailView`；未命中时返回空对象 `{}`。
- `id`：`Long`，策略主键 ID。
- `policyId`：`String`，策略业务标识。
- `policyName`：`String`，策略名称。
- `description`：`String`，策略说明。
- `enabled`：`Boolean`，是否启用。
- `currentVersionId`：`Long`，当前生效版本 ID。
- `currentVersionNo`：`String`，当前生效版本号。
- `includePromptKeys`：`Array<String>`，当前版本显式包含的 Prompt key 列表。
- `excludePromptKeys`：`Array<String>`，当前版本显式排除的 Prompt key 列表。

---

### 1. 接口名
保存 Prompt 策略

### 2. 方法类型
`POST`

### 3. 方法路径
`/api/prompt/policy/save`

### 4. 详细职责
该接口用于策略包的新建和更新。前端在策略创建页或策略编辑页点击保存时，应调用这个接口。源码会先创建或更新策略主记录，再新建一个新的 active 版本，并把旧 active 版本归档。因此每次保存实际上都会产生一个新的策略版本，不是就地覆盖。前端如果要在保存后刷新当前版本列表，应继续调用策略详情或版本列表接口。

### 5. 是否鉴权
是。
需要 `Authorization` 请求头中的有效 JWT。当前源码未发现额外角色权限控制。

### 6. 入参字段和类型
- 请求体为 `PromptPolicySaveRequest`。
- `policyId`：`String`，必填，策略业务标识。
- `policyName`：`String`，非必填，策略名称；新建时默认值为 `policyId`。
- `description`：`String`，非必填，策略说明；默认空字符串。
- `enabled`：`Boolean`，非必填，策略是否启用；源码默认按 `true` 处理，保存后也会把策略重新置为启用。
- `includePromptKeys`：`Array<String>`，非必填，本次版本要显式包含的 Prompt key 列表；不传时会保存为空数组，不会自动继承旧版本。
- `excludePromptKeys`：`Array<String>`，非必填，本次版本要显式排除的 Prompt key 列表；不传时会保存为空数组。
- `version`：`String`，非必填，版本号；不传时自动递增，默认起始值 `1.0.0`。
- `changeNote`：`String`，非必填，变更说明；默认空字符串。

### 7. 回参字段和类型
- 成功时返回 `PromptPolicyEntity`，注意这里返回的是策略主记录，不包含新版本的 include/exclude 内容。
- `id`：`Long`，策略主键 ID。
- `policyKey`：`String`，策略业务标识。
- `policyName`：`String`，策略名称。
- `description`：`String`，策略说明。
- `enabled`：`Boolean`，是否启用。
- `currentVersionId`：`Long`，新写入的当前生效版本 ID。
- `createdAt`：`String`，创建时间。
- `updatedAt`：`String`，更新时间。
- 重要说明：每次保存都会新建一个 active 版本，并把旧 active 版本归档。
- 常见失败原因会走统一异常结构，例如 `policyId is required`。

---

### 1. 接口名
删除 Prompt 策略

### 2. 方法类型
`POST`

### 3. 方法路径
`/api/prompt/policy/delete`

### 4. 详细职责
该接口用于策略管理页删除某个策略包。这里的删除也是软删除：源码不会删除版本数据，而是把策略主记录设为禁用，并在描述后面追加 `[deleted]` 标记。前端在列表页做删除确认时可直接调用；如果之后还要展示历史版本，仍然可以通过版本接口读取。

### 5. 是否鉴权
是。
需要 `Authorization` 请求头中的有效 JWT。当前源码未发现额外角色权限控制。

### 6. 入参字段和类型
- 请求体为 JSON 对象。
- `policyId`：`String`，必填，要删除的策略业务标识。

### 7. 回参字段和类型
- 成功时返回 `Object`。
- `success`：`Boolean`，固定为 `true`。
- `policyId`：`String`，本次删除的策略 ID。
- 重要说明：如果策略不存在，源码也会直接返回成功。
- 删除后策略主记录会被更新为 `enabled=false`，并在 `description` 末尾追加 `[deleted]`，但这些变更不在响应体中展开。
- 失败时通常返回统一异常结构，例如 `policyId is required`。

---

### 1. 接口名
查询 Prompt 策略版本列表

### 2. 方法类型
`GET`

### 3. 方法路径
`/api/prompt/policy/versions`

### 4. 详细职责
该接口用于策略版本管理页。当前端需要查看某个策略包的历史版本、包含/排除内容变化、当前激活版本以及后续执行激活操作时，应先调用这个接口。它返回策略版本历史列表，适合做时间线、版本表格或回滚选择器。

### 5. 是否鉴权
是。
需要 `Authorization` 请求头中的有效 JWT。当前源码未发现额外角色权限控制。

### 6. 入参字段和类型
- `policyId`：`String`，必填，策略业务标识。

### 7. 回参字段和类型
- 成功时返回 `Array<PromptPolicyVersionEntity>`。
- `[].id`：`Long`，策略版本 ID。
- `[].promptPolicyId`：`Long`，所属策略主记录 ID。
- `[].versionNo`：`String`，版本号。
- `[].includePromptKeys`：`Array<String>`，该版本包含的 Prompt key 列表。
- `[].excludePromptKeys`：`Array<String>`，该版本排除的 Prompt key 列表。
- `[].status`：`String`，版本状态，常见值 `active`、`archived`。
- `[].changeNote`：`String`，变更说明。
- `[].isActive`：`Boolean`，是否当前激活版本。
- `[].createdAt`：`String`，创建时间。
- `[].updatedAt`：`String`，更新时间。
- 返回顺序按 `createdAt desc`；策略不存在时返回空数组 `[]`。

---

### 1. 接口名
激活 Prompt 策略版本

### 2. 方法类型
`POST`

### 3. 方法路径
`/api/prompt/policy/activate`

### 4. 详细职责
该接口用于把某个策略版本切换为当前运行时生效版本。前端在策略版本列表页点击“激活”时应调用它。源码会先校验版本归属，再归档旧 active 版本，最后把目标版本置为 active，并把策略本身重新置为启用状态。适合做正式策略切换。

### 5. 是否鉴权
是。
需要 `Authorization` 请求头中的有效 JWT。当前源码未发现额外角色权限控制。

### 6. 入参字段和类型
- 请求体为 `PromptPolicyVersionSwitchRequest`。
- `policyId`：`String`，必填，策略业务标识。
- `versionId`：`Long`，必填，要激活的策略版本 ID。

### 7. 回参字段和类型
- 成功时返回 `Object`。
- `success`：`Boolean`，固定为 `true`。
- `policyId`：`String`，策略业务标识。
- `versionId`：`Long`，被激活的版本 ID。
- 激活成功后，策略主记录会被同步设为 `enabled=true`，但该字段不在响应体中展开。
- 失败时通常返回统一异常结构，例如 `policyId is required`、`versionId is required`、`policy not found`、`policy version not found`。
